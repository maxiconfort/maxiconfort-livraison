# Projet : App Livraison Pro (livraison.maxiconfort.fr)

PWA interne de gestion des livraisons Maxiconfort. **Ce dossier est le repo git actif** (GitHub `maxiconfort/maxiconfort-livraison`, déployé via GitHub Pages + CNAME).

## Stack
- App mono-fichier : `maxiconfort-v7.html` (~13 000 lignes, HTML/CSS/JS vanilla)
- Backend : Supabase projet `jmvfjtnmebstkzcfnlgp` (RLS x-app-secret, Edge Functions, pg_cron)
- SMS : **OVH** (migré de Brevo le 16/06, Brevo bloqué) · Cartes : Leaflet/OSRM · PWA : `service-worker.js` (**toujours bumper CACHE_VERSION à chaque déploiement du HTML**)
- Credentials : `.env` (jamais commit) · Déploiement Edge Functions autonome via token Supabase PAT (`SUPABASE_ACCESS_TOKEN` puis `npx supabase functions deploy <nom> --project-ref jmvfjtnmebstkzcfnlgp --no-verify-jwt`)
- Rôles login PIN : admin / CV / LV (livreur mobile)

## État au 23/06/2026 — NOUVEAU : SMS demande d'avis Google (J+1, automatique)
- **Demande Borhen : automatiser la demande d'avis Google après livraison.** ⚠️ Borhen voulait offrir un cadeau contre avis positif → **refusé et expliqué** (interdit Google = suspension fiche + illégal FR, pratique commerciale trompeuse/review gating). Retenu : **demande d'avis HONNÊTE, sans condition ni récompense.** Fiche Google Business créée le 23/06 (voir mémoire `project_seo_maxiconfort`/`project_service_client`), lien d'avis = `https://g.page/r/CbtQ5liS28rbEBM/review`.
- **Implémentation** : (1) nouveau type **`avis`** dans `send-cmd-sms` (template ≤149 car = 1 segment = 1 crédit OVH, **sans emoji ni accent** ; constante `LIEN_AVIS`) ; (2) nouvelle Edge Function **`sms-avis`** (mirroir de `sms-auto`) : scanne `commandes` `date_livraison = J-1 (Paris)` + `statut='livré'` + tél valide + hors `#SAV` + pas déjà sollicitée (dédupe `sms_envoyes` type `avis`) → appelle `send-cmd-sms {type:'avis'}`. Body `{dryRun?, dateCible?}` (dryRun = liste sans envoyer). (3) cron pg_cron **`sms-avis-daily`** (jobid 8, `0 9 * * *` UTC = **11h Paris été**) via migration `011_pg_cron_sms_avis.sql`. Déployées + cron créé via Management API.
- **Message** : `Bonjour {prenom}, merci pour votre achat chez Maxiconfort ! Votre avis en 1 clic : <lien> Merci de votre confiance.`
- **Testé en dryRun** (sans envoi) : 23/06→5 clients, 22/06→2, 20/06→9, filtrage OK. **N'envoie PAS l'historique** (uniquement J-1 chaque jour). 1er envoi réel = demain 11h pour les livrés d'aujourd'hui. ⚠️ ~1 crédit OVH/client livré → garder le solde OVH alimenté. Pas de bump SW (aucune modif HTML).
- **Pour couper** : `SELECT cron.unschedule('sms-avis-daily');`. Désactivation auto possible si solde OVH bas (non implémenté).
- **MAJ sms-avis v1.1 (24/06) — exclusion "client à risque"** : si la note de commande (`instr`) contient **« PAS D'AVIS » / « SANS AVIS » / « NO AVIS »** (helper `noteExclut`, insensible casse/ponctuation), la commande est **sautée** (action `skip_exclu_note`). Borhen écrit ce mot-clé dans la note **avant 11h le lendemain** de la livraison (réflexe : le noter le jour même si ça s'est mal passé). Testé node 10/10 (exclut les 3 variantes, 0 faux positif sur impératif horaire/code/étage). ⚠️ Pour bloquer un client déjà parti : trop tard (SMS envoyé). Pour bloquer rétroactivement avant l'envoi : pré-marquer `sms_envoyes` avec un objet `{type:'avis'}` (la dédup le saute).

## État au 15/07/2026 — NOUVEAU : gardien SMS des commandes GLS sans étiquette (alerte-gls)
- **Contexte : le 08/07, ~11 commandes GLS payées étaient restées SANS étiquette** (jusqu'à 1 mois de retard — Farges #1294 du 16/06 —, 1 réclamation client par e-mail : Tondereau #1381). Cause : Borhen expédie parfois **en direct sur YourGLS** (compte SOCIETE BMS) sans passer par l'app → pas de tracking en base, pas de SMS client, Shopify non synchronisé, et la commande semble « oubliée ». Purge faite le 15/07 : SAV Medda rattaché à ses suivis YourGLS (`00L3XDZR,00L3XDZS`), 4 étiquettes créées via l'app (#1294 Farges 3 colis, #1344 Conrod 3, #1353 Gourlet 3, #1394 Bouaiss **9 colis corrects** = 3 matelas + 3 sommiers 160x200).
- **Gardien : nouvelle Edge Function `alerte-gls`** (`supabase/functions/alerte-gls/index.ts`) : scan `commandes` `transporteur='GLS'` + `statut='en-attente'` + `tracking_transporteur` vide + `date_commande <= J-2` → **SMS à Borhen** (+33744289321) avec la liste (max 6 + compteur). Dédupe 1 alerte/jour via `sms_historique` type `alerte-gls` (re-alerte chaque jour tant que non traité). Body `{dryRun?, force?, seuilJours? (défaut 2)}`. **Cron pg_cron `alerte-gls-daily`** (jobid 13, `30 6 * * *` UTC = 8h30 Paris été) via migration `012_pg_cron_alerte_gls.sql` (posée via Management API). Testé dryRun : « rien a signaler » (arriéré purgé). ~1 crédit OVH par jour d'alerte. Pas de bump SW (aucune modif HTML). Commit 11bfc08.
- ⚠️ **Règle de travail** : avant de créer une étiquette pour une commande qui traîne, **toujours vérifier YourGLS** (l'expédition existe peut-être déjà en direct → doublon facturable). Réflexe recommandé : passer par le bouton de l'app pour TOUTES les expéditions GLS.

## État au 27/07/2026 — gls-sync v13 : les statuts GLS remontent à nouveau (règle 100 % colis assouplie)
- **Constat Borhen : « les statuts des livraisons GLS ne remontent pas ». Vérifié : AUCUNE livraison GLS détectée depuis le 16/07**, 19 commandes en attente dont 14 « bloquées ». Cause : `gls-sync` ne passait une commande en `livré` que si **`nbLivre === nbColis`** (100 % des colis scannés livrés). Or **GLS ne scanne pas toujours tous les colis à la livraison** (ex #1438 Xavier 2/3 livrés, #1463 Julie 1/2 — le colis restant sans scan depuis des jours) → la commande restait « en-attente » pour toujours. **FIX (v13)** : `tousLivre = (0 erreur) && (nbLivre === nbColis || (nbLivre >= 1 && resteSilencieux))` avec `resteSilencieux` = aucun scan sur les colis restants depuis **48h** (les colis d'un même envoi voyagent ensemble). Déployé + sync réelle : **10 commandes passées en livré** (#1463 Julie, #1438 Xavier, #1441 Carrere, #1449 Kherzane, #1386 Valentini, #1362 Correia, #1373 Senet, **#1025 Nadège Laot** (saga terminée !), #1439 Raignoux, #1448 Florent Simon GLS). `shopify-fulfill` (cron 20 min) synchronisera les commandes site concernées.
- **⚠️ RESTE 5 commandes VRAIMENT bloquées (aucun scan GLS depuis la création de l'étiquette)** : **#1381 Tondereau (étiq 10/07 !, la cliente qui avait réclamé par email)**, #1344 CONROD, #1294 Farges, #1353 Gourlet, #1394 Bouaiss (étiq 15/07 = le lot de la purge). 12-17 jours sans le moindre scan d'enlèvement → soit expédiées **en direct sur YourGLS** (autres n° → vérifier YourGLS), soit **jamais parties** (vérifier le dépôt). Action Borhen requise. Les autres en transit (Louise 24/07 qui bouge, Myrhiam/VANDIER/Germain étiquetées le 27/07) sont normales.

## État au 20/07/2026 — Bot WhatsApp : « matelas 90x190x15 » ne devient plus un lit superposé
- **Constat Borhen : commande #1461 (Grégoire VANDIER, saisie via le bot WhatsApp) devait être un « Matelas 90×190×15, 119 € » mais a été enregistrée comme « Lit Superposé Métallique DUO 90×190 + 2 Matelas DODOCONFORT 90×190×15 ».** Cause dans `whatsapp-receive` (`chercherProduit`/`detecterCategorie`) : le nom du DUO **contient « Matelas … 90×190×15 »** → `detecterCategorie` (qui testait `matelas` AVANT `superpose`) le classait **catégorie « matelas »** → dimensions identiques + mêmes mots = **égalité de score** avec le vrai matelas → l'ordre du catalogue décidait (le DUO gagnait). **FIX (whatsapp-receive v2.1)** : (1) `detecterCategorie` réordonné — les produits **composés d'abord** (`ensemble/pack` → `superpose` → `coffre` → `lit nico`/`lit` → `matelas+sommier`=ensemble → `matelas` → `sommier` → `canape`) ; (2) **malus −25** si catégorie demandée ≠ catégorie produit (un « matelas » ne matche plus jamais un lit superposé même à dimensions égales). Testé node **9/9** (matelas 90×190×15 → 82 pts vs DUO 27 pts ; superposé/ensemble/sommier/NICO/coffre tous corrects). Déployé. **Donnée corrigée** : #1461 → produit + ligne « Matelas DODOCONFORT 90×190×15 » (`produitId=pr1779220886089`, prix vente 119 conservé) ; le stop de tournée éventuel se resynchronise via v7.5.103. Pas de bump SW.

## État au 20/07/2026 — Garde-fou v1.1 : dates verrouillables (exception Borhen)
- **Suite immédiate : Borhen a promis « mercredi 22/07 » aux 4 clients reportés → le cron du 21/07 18h ne doit PAS re-décaler.** **v1.1 de `garde-tournee-min`** : avant le comptage, la fonction lit `parametres.cle='gardefou_skip_dates'` (valeur = JSON array de dates `YYYY-MM-DD`) ; si `dateCible` y figure → `action:'rien', raison:'date verrouillee par Borhen'` (le paramètre `force:true` passe outre). **Exception posée en base : `["2026-07-22"]`**. Testé dryRun cible 22/07 → bien verrouillé. Redéployé. **Usage futur** : pour garantir une tournée malgré < 10 clients, ajouter sa date au tableau (upsert `parametres` on_conflict=cle) ; pour re-suspendre le garde-fou un soir, idem. Les dates passées dans le tableau sont inertes (pas de nettoyage nécessaire, mais possible).

## État au 20/07/2026 — NOUVEAU : garde-fou rentabilité tournée (< 10 commandes = report auto + SMS)
- **Demande Borhen : si une tournée n'atteint pas 10 commandes minimum, la décaler au lendemain automatiquement, avec SMS d'excuse aux clients.** Nouvelle Edge Function **`garde-tournee-min`** (`supabase/functions/garde-tournee-min/index.ts`) : scanne les commandes **RANOU `en-attente`** de la date cible (défaut **demain** Paris, hors `#SAV`) ; si `nb < seuilMin` (défaut **10**) → chaque commande est **reportée** (`date_livraison` = date+1 par défaut), ses **drapeaux SMS tournée réinitialisés** (`veille/depart/route/proche` retirés de `sms_envoyes`, mécanique v7.5.90), le client reçoit un **SMS d'excuse** (« reportee a {jour} {JJ/MM}. Toutes nos excuses pour ce contretemps » — ~130 car = 1 crédit, log `sms_historique` type `report`), et **Borhen reçoit un récap** (type `report-gardefou`). Body `{dryRun?, seuilMin?, dateCible?, nouvelleDate?, force?}`. **Cron `garde-tournee-min-daily`** `0 16 * * *` UTC = **18h Paris** chaque soir (AVANT la veille de 19h → les reportés ne reçoivent pas la veille à tort). Si le lendemain est encore < 10, le cron du lendemain re-décale (accumulation jusqu'à rentabilité). Les tournées ne sont pas modifiées par la fonction (le `reconcilierTournees` du front re-range au chargement).
- **Application immédiate (demande Borhen)** : la tournée de demain **mardi 21/07 (T-51) n'avait que 4 clients** (Hammouti #1456, Victor #1458, Qasmi #1460, Patricia Nalet #1444) → **reportée à mercredi 22/07** via la fonction : 4 commandes re-datées + 4 SMS d'excuse envoyés (tous OK) + récap à Borhen. **T-51 re-datée 22/07** en base ; pendant l'opération l'app ouverte de Borhen a vidé les stops (reconcilier sur dates transitoirement incohérentes) → **stops re-injectés à la main** (4/4, vérifié). ⚠️ En cas de report groupé futur : la fonction seule suffit, le front re-range tout seul.

## État au 15/07/2026 (version v7.5.103) — Tournées : la fiche du stop suit les modifs de la commande
- **Constat Borhen : client Qasmi (#1460, tournée T-51 du 21/07) affichait « Lit Superposé DUO » dans la vue tournée alors que sa commande (modifiée après coup) contient « Ensemble Lit NICO Noir 140×190 ».** Cause : un stop de tournée prend une **photo** de la commande (produit/prix/adresse/tel/etage/asc/code/instr) au moment de l'ajout ; `reconcilierTournees` synchronisait les statuts/dates mais **jamais ces champs fiche** → une commande éditée ensuite laissait la tournée sur l'ancienne photo. **FIX (v7.5.103)** : dans `reconcilierTournees` PASSE 1, branche « garder » (~ligne 8311), les 8 champs `['produit','prix','tel','adresse','etage','asc','code','instr']` sont **rafraîchis depuis la commande** si différents (`change=true` → sauvegarde). Les stops **livrés** ne passent pas par là (retournés plus haut) → historique intact ; `paie`/`stpaie` restent gérés par le polling v7.5.51 + signature (pas touchés ici, pas de conflit). **Donnée corrigée** : stop #1460 de T-51 patché en base (produit NICO, 279 €). Parse OK. Bump SW v7.5.103.
- **Contexte du jour (15/07) : PANNE SUPABASE — quota gratuit épuisé (« Egress Exceeded », HTTP 402 sur tout).** App + tous les crons SMS morts depuis la nuit. **Borhen a upgradé au plan Pro (25 $/mois)** → rétabli en minutes. Dégâts : aucun (pas de tournée le 15/07, confirmations parties avant la coupure) ; seul le rapport matin 7h30 a sauté (renvoyé en `force`). Base **lente** (~4-20 s/requête) pendant ~1h post-rétablissement (+ incident technique Supabase concomitant) → si l'app paraît « cassée » juste après, c'est ça. ⚠️ Solde OVH = **325 crédits** (~3-4 semaines) → recharger bientôt. 📌 À faire (promis à Borhen) : **optimiser l'egress** (polling 10 s gourmand, purge/déport des `gls_pdf_base64` en Storage).

## État au 08/07/2026 (version v7.5.102) — Non encaissé : clic client → fiche commande
- **Demande Borhen : dans la section « Non encaissé » (onglet À traiter des Encaissements), cliquer un client doit ouvrir sa fiche commande.** **FIX (v7.5.102)** : dans le tableau `aEncaisser` (`finRender`, ~ligne 10661), les cellules **Client** (soulignée, curseur pointeur) et **Produit** ont `onclick="ouvrirCmd(c.id)"` (même mécanique que v7.5.89 pour les stops de tournée) → ouvre le panneau commande (statuts + Modifier + paiement). Les **selects Mode/Statut** de la même ligne restent indépendants (pas de clic parasite). Parse OK. Bump SW v7.5.102.

## État au 08/07/2026 (version v7.5.101) — Encaissements simplifiés : 3 sous-onglets
- **Demande Borhen : simplifier la rubrique Encaissements (10 sections empilées, l'opérationnel noyé dans l'analyse).** Concept validé par Borhen via mockup : **3 sous-onglets** dans `fin-section-enc` (le sélecteur de période reste au-dessus, commun) — **(1) « 💵 À traiter »** (défaut, cartes dépliées) = 4 KPI (`fin-ca/enc/dep-kpi/net`) + **retours de tournée** (`fin-caisse`, remonté en tête) + **alerte non encaissé** (`fin-enc-alert`) + **Dépenses** (carte pleine largeur avec nav mois/filtres/+Ajouter) ; **(2) « 📊 Analyse »** = graphique évolution mensuelle (`fin-graph`) + répartition encaissements (`fin-repartition` + bouton Modifier un encaissement) + détail par livreur (`fin-livreurs`) + CA par jour (`fin-ca-jour`) ; **(3) « 📜 Journal »** = journal complet avec ses filtres (`fin-journal`). **Implémentation** : pur déplacement de blocs HTML dans 3 conteneurs `fin-sub-{traiter|analyse|journal}` + fonction **`showFinSub(key)`** (près de `resetFiltresJournal`) qui bascule le `display` — **AUCUN calcul modifié** (`finRender()` remplit les mêmes IDs, un bloc en `display:none` est rendu quand même). Onglets Rentabilité/Zones (`showFinTab`) **intacts**. Vérifié : parse OK, **22 IDs critiques uniques** (pas de doublon après déplacement), équilibre `<div>` parfait (1488/1488). Bump SW v7.5.101. ⚠️ Borhen doit mettre l'app à jour (cache).

## État au 08/07/2026 (version v7.5.100) — Vue livreur : bouton Annulé + FIX mode paie CB→Espèces
- **Demande Borhen (2 points sur la Vue livreur).** **(1)** Ajout du statut **« Annulé »** dans la vue livreur (le livreur peut annuler un client qui annule) : ajouté `{k:'annulé', ico:'❌', lbl:'Annulé'}` à `stDefs` (~ligne 8733) + couleur `'annulé': {bg:'#5F5E5A', bdg:'b-gray', btn:'a-gray'}` à `stColors`. Le clic appelle `setLvSt(tid, idx, 'annulé')` (déjà géré : met stop+commande en annulé, `reconcilierTournees` retire le stop au rechargement v7.5.88). **(2) FIX critique — le mode CB remontait en Espèces.** Cause : DEUX endroits pour le mode de paiement — le **sélecteur** `lv-paie-change` sur la carte (avant livraison, `chgStopPaie` met `s.paie`/`cmd.paie`) ET le **modal signature**. Quand le livreur mettait « CB » sur la carte puis cliquait « Livré », `setLvSt` branche livré (~ligne 9711) forçait **`sigSelectMode('Espèces')`** → `validerSig` écrasait `s.paie`/`cmd.paie` avec `sigPaieMode='Espèces'` → tout remontait en Espèces (caisse faussée). **FIX (v7.5.100)** : `var _modeInit = (s.paie === 'CB' || s.paie === 'Mixte') ? s.paie : 'Espèces'; sigSelectMode(_modeInit);` → le modal reprend le mode déjà choisi sur la carte (le livreur peut encore changer). Parse OK (7 statuts, fix présent). Bump SW v7.5.100. ⚠️ Borhen/RANOU doivent mettre l'app à jour (cache). ⚠️ Les livraisons **déjà** enregistrées à tort en Espèces avant ce fix restent à corriger à la main si besoin (le forward est corrigé).

## État au 30/06/2026 (version v7.5.99) — Refonte « Retours de tournée » (Encaissements)
- **Demande Borhen : la section « retours de tournée » (page Encaissements) est confuse/pas fonctionnelle → refonte en checklist claire.** Concept validé par Borhen (via mockup) : (1) **en-tête récap** « X à remettre · Y € » (somme des `aRemettre` des tournées non validées) ; (2) **cartes « à traiter »** en haut (bordure orange) avec calcul en 3 lignes — `+ Espèces encaissées`, `− Dépenses payées en espèces` (avec détail cat/comment), `= À remettre` en gros (24px vert) + rappel « déjà encaissé, rien à rendre » (CB/virement/prépayé) + **1 seul bouton** « ✅ RANOU a remis X € — valider » ; (3) tournées **sans espèce à remettre** = lignes grises compactes ; (4) **retours validés repliés en bas** (lignes vertes + « ↩ Annuler »). **FIX (v7.5.99)** : bloc `finRender` (~ligne 10511) — `tourneesLv.map` renvoie maintenant un **objet info** (au lieu du HTML direct), puis split `aTraiter`/`traitees`/`rienARem` + rendu. **AUCUN calcul modifié** : `getMontantEspecesCmd`/`getMontantCBCmd`, filtre dépenses espèces (`d.livreur===lv`, `!==CAISSE`, `!==CB`, match tournée par `tourneeId` sinon date), `aRemettre = max(0, especes − depEspLv)` — tout identique. `validerRemiseCaisse`/`annulerRemiseCaisse` (table `caisse_remises`) **inchangés**. Choix Borhen : valider le montant **calculé** (pas de saisie du réel). Parse OK. Bump SW v7.5.99. ⚠️ Borhen doit mettre l'app à jour (cache).

## État au 30/06/2026 (version v7.5.98) — Tournées : montants € masqués pour les collaboratrices
- **Demande Borhen (connecté en accès Collaboratrice) : dans Tournées, les collaboratrices ne doivent PAS voir les montants € (ni le « X € encaissés » des cartes, ni le « 349 € Espèces » par client, ni le total) — juste la progression (nb livraisons / clients).** **FIX (v7.5.98)** : 3 montants conditionnés à `role === 'admin'` dans le fichier : (1) carte de tournée `renderTournees` (~ligne 8500) `✓ X € encaissés` ; (2) header détail `afficherTournee` (~ligne 17570) `td-total` → `—` si non-admin ; (3) ligne montant par stop `afficherTournee` (~ligne 17591) `s.prix € + s.paie`. **Gardé pour tous** : progression `📦 done/total`, badges `X clients · rentable`, nb clients (la rentabilité est basée sur le NOMBRE de clients ≥13, pas sur un €). Rôles : admin voit les €, CV (collaboratrice) et tout autre rôle ne les voient pas (cohérent avec le dashboard `role==='admin' ? … : '—'`). Parse OK. Bump SW v7.5.98. ⚠️ La collaboratrice doit mettre l'app à jour (cache).

## État au 30/06/2026 (version v7.5.96) — Compteurs Commandes = périodes sélectionnables (comparaison)
- **Demande Borhen : transformer les 3 compteurs (Aujourd'hui / Semaine / Mois) de la page Commandes en sélecteurs pour voir les chiffres des périodes PRÉCÉDENTES et comparer.** Avant : 3 badges figés sur la période en cours (`renderCmdStatsJour`, ~ligne 5415). **FIX (v7.5.96)** : chaque carte a maintenant un sélecteur — **Jour** = `<input type=date>` (max=aujourd'hui), **Semaine** = `<select>` des 8 dernières semaines (lundi→dimanche), **Mois** = `<select>` des 12 derniers mois. Au changement → `cmdStatsSel.{jour|sem|mois}` mis à jour + re-render. **Comportement par défaut INCHANGÉ** : si le sélecteur est sur la période en cours, les chiffres viennent du fetch du mois déjà fait (`statsPeriode`/`vraies`) — identique à avant (vérifié : mêmes bornes, pas de date future en base). Pour une période **passée**, fetch dédié **`cmdFetchRange(dMin, dMaxExcl)`** (mêmes filtres : hors annulé, hors SAV `origine`, hors rétroactif `date_livraison<date_commande`) ; les plages **entièrement passées** (immuables) sont **mises en cache** (`cmdStatsCache`) pour ne pas re-fetcher. **N'altère AUCUN calcul financier** (caisse/encaissements/CA intacts — affichage seul). Parse OK + testé node (12 mois, 8 semaines, bornes correctes). Bump SW v7.5.96. ⚠️ Borhen doit mettre l'app à jour (cache).

## État au 29/06/2026 (version v7.5.95) — Dépense espèces sans livreur = non déduite (FIX)
- **Constat Borhen : « je marque les dépenses du 27/06 mais ce n'est pas pris en compte dans encaissements RANOU ».** Diagnostic : la dépense **430€ Achat fournisseur (STEFAN, 27/06, espèces)** avait le champ **`livreur` VIDE**. Or le calcul caisse par tournée (`finRender` ~ligne 10467) déduit une dépense espèces seulement si `d.livreur === lv` (RANOU) — une dépense sans livreur n'est déduite de la caisse de **personne** → l'argent « disparaît » du calcul. (Confirmé : l'app affichait « 27/06 · Espèces » sans « RANOU » pour le 430€.) **Données corrigées** : 430€ (27/06) + 510€ (11/06) orphelines → `livreur=RANOU` en base (PATCH). T-35 (27/06) : 957€ espèces − 530€ (100+430) = **427€ à remettre**. **FIX racine (v7.5.95)** : dans `sauvegarderDepense` (~ligne 10993), garde-fou — une dépense **ESPÈCES** enregistrée **sans livreur** est rattachée par défaut à **RANOU** (`if (!_depLivreur && depModePaie !== 'CB') _depLivreur = 'RANOU'`). Les dépenses **CB** (compte société) gardent « Général » (pas d'impact caisse espèces). Parse OK. Bump SW v7.5.95. ⚠️ Borhen doit **mettre l'app à jour** (cache tenace, cf. saga Dembélé) pour que le garde-fou s'applique aux nouvelles saisies. ⚠️ Le select `dep-livreur` garde l'option « — Général — » (value="") par défaut à l'affichage ; le garde-fou agit à la sauvegarde.

## État au 29/06/2026 — Lit superposé DUO = 2 colis (multi-colis GLS)
- **Constat Borhen : commande #1346 (Mathieu Jobst, « Lit Superposé Métallique DUO 90x190 ... 2 personnes », site, GLS) → l'app générait 1 seul BL GLS au lieu de 2.** Cause : `MULTICOLIS_RULES` (gls-create-shipment) n'avait **aucune règle pour les lits superposés** (que lit coffre, lit NICO, sommiers, matelas) → `detectMultiColis` renvoyait null → 1 colis par défaut. **FIX** : ajout règle **`/lit\s*superpos[\s\S]*?(duo|2\s*places|2\s*personnes)/i` → 2 colis [20,20]** (placée après Lit NICO). Cible le **DUO (1+1, 2 personnes)** et **PAS** le modèle « 2+1 Places / 3 Personnes » (90×140/190) dont le nb de colis reste **à définir avec Borhen** (question posée, dismissée — à reprendre). Testé dryRun #1346 → **2 colis** (1/2, 2/2). Déployé. Pas de bump SW. **#1346 débloqué** (tracking 00L2QLQG vidé + pdf + gls_date_etiquette + flag `expedition` retiré de sms_envoyes) → Borhen recrée l'étiquette (2 colis) ; le SMS expédition repartira auto avec le nouveau tracking (flag vidé). Jeter l'ancienne étiquette 1 colis.

## État au 27/06/2026 — Synchro Shopify pour les livraisons RANOU (IDF) — le trou est bouché
- **Constat Borhen : commande site #1034 (Quentin Thery, Le Plessis-Trévise 94, IDF) livrée par RANOU ce matin, mais restée « Non traitée » sur Shopify.** Cause : la synchro fulfillment du 24/06 (`syncShopifyFulfillment` dans `gls-create-shipment`) ne se déclenche **qu'à la création d'une étiquette GLS**. Une commande **IDF livrée par RANOU** n'a pas d'étiquette → jamais synchronisée → reste « Non traitée » sur le site. **FIX : nouvelle Edge Function `shopify-fulfill`** (`supabase/functions/shopify-fulfill/index.ts`) = marque « Traitée »/Fulfilled sur Shopify les commandes **site livrées** (RANOU **ou** GLS). Body `{cmdId?, dryRun?, fenetreHeures?}`. Par cmdId (immédiat) ou scan des commandes `statut='livré'` + `ref_marketplace` numérique livrées dans les `fenetreHeures` (défaut 72). Pour chaque : GET `fulfillment_orders` open → si aucun = déjà traitée (skip, idempotent) → POST `/fulfillments.json`. **GLS** : `tracking_info {number:1er GLS, company GLS, url}` + `notify_customer:true`. **RANOU** : pas de n° transporteur, lien `track.html?t=token` comme url, **`notify_customer:false`** (commande déjà livrée + client déjà notifié par SMS de l'app → pas d'email Shopify tardif/incohérent). **Cron `shopify-fulfill-20min`** `*/20 5-19 * * *` UTC (= 7h–21h Paris, toutes les 20 min, fenêtre 24h). **#1034 (app #1343, ref 17841720262986) traité immédiatement** → `ok:true`, fulfillment_id 8282791117130. ⚠️ Réutilise les scopes fulfillment du token Shopify (ajoutés le 24/06). ⚠️ Le déclencheur GLS (`gls-create-shipment` v5.13) reste en place ; `shopify-fulfill` est idempotent (skip si déjà traitée) donc pas de conflit. Pas de bump SW.

## État au 24/06/2026 — Synchro statut Shopify (commande site → « Expédiée ») à la création d'étiquette GLS
- **Demande Borhen : quand une commande du SITE part avec BL GLS édité, synchroniser son statut sur Shopify en temps réel.** Choix Borhen : **email Shopify au client = OUI** (`notify_customer:true`, en plus du SMS GLS). **Implémenté dans `gls-create-shipment` v5.13** : nouvelle fonction **`syncShopifyFulfillment(cmd, trackId)`** appelée dans le bloc succès (après le SMS expédition), **non bloquante** (try/catch isolé → n'empêche jamais la création d'étiquette). Logique : si `cmd.ref_marketplace` = ID Shopify numérique (= commande site) → GET `/orders/{id}/fulfillment_orders.json` (filtre status open/in_progress/scheduled) → POST `/fulfillments.json` avec `line_items_by_fulfillment_order` + `tracking_info {number: 1er trackGLS, company:'GLS', url}` + `notify_customer:true`. Résultat renvoyé dans la réponse (`shopifySync`). Secrets Shopify réutilisés de `shopify-sync` (`SHOPIFY_STORE_DOMAIN/ACCESS_TOKEN/API_VERSION`). Ajout action **`testShopify`** (lecture seule, GET fulfillment_orders) pour diagnostiquer le token sans rien marquer. Déployé. Pas de bump SW.
- **✅ RÉSOLU (24/06) : scopes fulfillment ajoutés au token Shopify.** Le token (app « Claude Assistant Maxiconfort », gérée dans le **Dev Dashboard** Shopify — `dev.shopify.com/dashboard/.../apps/...`) n'avait que `read_orders`/`write_orders`, pas de scope fulfillment → `testShopify` HTTP 403. **Procédure suivie (guidée en computer-use, Borhen clique)** : Dev Dashboard → app → **Versions** → « Création de version » (basée sur la v5 active) → section **Accès / Champs d'accès** (zone CSV éditable) → ajouté à la fin des **scopes OBLIGATOIRES** (PAS facultatifs — un token backend n'obtient que les requis) : `read/write_assigned_fulfillment_orders`, `read/write_fulfillments`, `read/write_merchant_managed_fulfillment_orders`, `read/write_third_party_fulfillment_orders` → **Publier** (devient v6 active) → page Aperçu → **« Installer l'application »** → choisir la boutique Maxiconfort.fr → écran « Mettre à jour l'accès aux données » → **« Mettre à jour »** (accorde les nouveaux scopes). **Le token n'a PAS changé** (réinstall = ajout de scopes au token existant) → `shopify-sync` intact, secret `SHOPIFY_ACCESS_TOKEN` inchangé. **Re-test `testShopify` #1330 → `ok:true` HTTP 200** + fulfillment_order `open` retourné. La synchro `syncShopifyFulfillment` (POST `/fulfillments.json`, `notify_customer:true`) est donc opérationnelle : à la **prochaine création d'étiquette GLS sur une commande site**, elle marquera la commande « Expédiée » sur Maxiconfort.fr + email client avec suivi GLS. Vérifier le champ `shopifySync` dans la réponse de `gls-create-shipment` au 1er vrai cas. ⚠️ Le **vrai test write** (POST fulfillment) ne sera fait qu'à la 1re vraie étiquette site (ne pas marquer une commande expédiée à blanc). Action `testShopify` (lecture seule) conservée pour diagnostics futurs.

## État au 24/06/2026 (version v7.5.94) — Un SAV ajouté à la main reste dans la tournée
- **Demande Borhen : « j'ai rajouté Dembélé » (SAV échange `#SAV9298411`, lit superposé, RANOU, 25/06) mais il n'apparaissait pas dans la tournée T-27.** Cause : `reconcilierTournees` PASSE 1 (~ligne 8308) **retirait tout stop SAV** à chaque rechargement (`String(cmd.id).indexOf('#SAV')===0` dans `horsTournee`). Donc un SAV à livrer par RANOU (échange à domicile) ne pouvait pas tenir dans une tournée. **FIX (v7.5.94)** : retiré la condition SAV de `horsTournee` → on ne retire plus que **GLS** (province expédiée) et les commandes **sans date**. Les SAV restent **exclus de l'auto-AJOUT** (PASSE 2 via `cmdEligibleTournee` qui garde `indexOf('#SAV')===0 → false`), mais un SAV **ajouté manuellement** à une tournée y **reste** désormais. Parse OK. Bump SW v7.5.94. **Données** : Dembélé inséré en base dans T-27 **position 5** (entre Cornil/Mennecy 91 et Siby/Paris — Villeneuve-St-Georges 94 = SE proche, sur le retour vers Paris) → T-27 = 9 stops. SMS veille déjà parti (sms_envoyes=[veille]). ⚠️ Le SAV a `prix=0`, `paie='Déjà payé'`, `stpaie='Payé'` (échange, rien à encaisser).

## État au 24/06/2026 (version v7.5.93) — Optim : créneau d'après-midi plus épinglé en 1er (anti-zigzag)
- **Bug signalé par Borhen sur la tournée du 25/06 (T-27, 8 stops très dispersés) : « le tri ne me convient pas… ça s'est dérèglé encore ».** Cause racine dans **`optimOrdonnerCreneaux`** (~ligne 12217) : la règle v7.5.77 « l'impératif à `due` (fin de créneau) minimale est épinglé en **position 1** » s'appliquait à **TOUT** impératif. Or le seul impératif de T-27 = **Mme Siby Aury, créneau 13h-16h** (`due`=960) → épinglée en 1er → la tournée **démarrait l'après-midi par elle** (matinée gâchée) + zigzag sur le reste. Et à chaque clic « Optimiser », l'app re-générait ce mauvais ordre et **écrasait** toute correction manuelle (y compris un réordonnancement écrit en base). **FIX (v7.5.93)** : on n'épingle en 1er **QUE** les impératifs **matinaux** — `if (forced == null || bestDue > SEUIL_EPINGLE_MATIN) return meilleur(...)` avec `SEUIL_EPINGLE_MATIN = 12*60` (midi). Un créneau d'après-midi (fin > 12h) repasse par l'**optimisation normale à fenêtres horaires** (`optimAmeliorerCreneaux`/`optimCoutCreneaux`, v7.5.74 : pénalité si arrivée après `creneauFin`, attente si avant `creneauDebut`) qui le place à la bonne heure sans gâcher la matinée. Les impératifs matinaux (« avant 9h30 », « avant midi », due ≤ 720) gardent l'épinglage strict en 1er (sécurité v7.5.77 préservée). Parse OK. Bump SW v7.5.93. **Données** : ordre de T-27 corrigé à la main en base (Arias→Babicz→Taban→Cornil→Siby→Bretegnier→Nicolas→CHEVILLARD : lointains SE le matin, Siby ~13h30 dans son créneau, fin au NO vers domicile RANOU). ⚠️ Réordonner directement en base est **fragile** : tant que l'app a la tournée en mémoire et que Borhen clique « Optimiser »/« Appliquer cet ordre », elle réécrit → la vraie correction = le fix de l'optimiseur. ⚠️ T-27 reste une tournée **exceptionnellement étalée** (Souppes/Varennes SE à ~1h30, Magny-en-Vexin NO à ~1h, Villepinte NE — 3 coins opposés + créneau Paris) → très longue journée quel que soit l'ordre ; option proposée à Borhen : scinder (sortir les plus excentrés sur un autre jour).

## État au 24/06/2026 (version v7.5.92) — Clic Waze = passage auto en « En route »
- **Demande Borhen : « RANOU ne clique jamais sur le bouton En route, je le connais » → le clic sur Waze doit déclencher le statut tout seul.** Avant, le clic Waze ouvrait juste la navigation + envoyait le SMS route ; la commande **restait `en-attente`** → le **suivi temps réel ne s'ouvrait pas** pour le client (`track-livreur` n'autorise la position que si statut `en-route`/`arrivé`). **FIX (v7.5.92)** : nouveau helper **`wazePasserEnRoute(cmdId)`** (~après `setLvSt`, ~ligne 9684) = trouve le stop par `cmdId` dans `tournees` et, **si encore `en-attente`**, appelle `setLvSt(tid, idx, 'en-route')` (qui synchronise stop+commande+tournée, sauvegarde en base, et gère le SMS départ groupé — dédupe serveur = pas de double SMS). Branché aux **3 chemins Waze** : bouton Waze par client (`lv-waze-btn` ~8782), `lancerWazeProchainStop`, `ouvrirWazeTournee`. Ne régresse jamais un `livré`/`arrivé`. Conséquence : dès que RANOU clique Waze pour un client, ce client **passe en route → suivi temps réel actif + SMS d'approche armé**. Parse OK, 4 occurrences `wazePasserEnRoute` (1 def + 3 appels). Bump SW v7.5.92. ⚠️ Nécessite que RANOU ait la position GPS active (app Android `app.apk` + GPS « toujours autorisé ») pour que le suivi montre réellement le camion.

## État au 24/06/2026 — SMS expédition raccourci (1 seul n° de suivi)
- **Demande Borhen (après recréation d'étiquettes multi-colis) : le SMS expédition listait TOUS les n° de tracking** (ex Nadège Laot 10 colis → SMS de 2-3 segments = 2-3 crédits + lien `?match=` illisible avec 10 n°). **FIX `send-cmd-sms` v3.2** : on ne garde que le **1er n° de suivi** (`tracking_transporteur.split(',')[0]`, suffit chez GLS pour suivre tout l'envoi) + le **nombre de colis** entre parenthèses. Template = « Bonjour X, commande #Y expediee par GLS (N colis). Suivi colis <1er n°> : <lien match=1er n°> ». Mesuré : **1 crédit** même à 10 colis (141 car). Déployé. Pas de bump SW. ⚠️ Le suivi GLS du 1er colis affiche l'ensemble de l'envoi groupé. **Contexte** : ce jour, après le fix ville, recréation des étiquettes #1310/#1029/#1025 (nouveaux trackings) → les SMS expédition initiaux (10h15-10h24, anciens n°) ont été **renvoyés avec le bon n°** (flag `expedition` vidé puis `send-cmd-sms` relancé). Les SMS expédition partent bien (vérifié `sms_historique` : 5 le 24/06 + 1 le 23, tous « envoyé »).

## État au 24/06/2026 — FIX étiquette GLS : ville = « Paris » à tort (gls-create-shipment)
- **Bug signalé par Borhen : toutes les étiquettes/BL GLS sortaient avec la ville « Paris »** (le nom, la rue, le code postal étaient bons, seule la ville était fausse). Cause racine dans `parseAdresseFR` (~ligne 98) : `const city = afterZip.split(/[,\n]/)[0]`. Les adresses au format **« Rue, CP, Ville, France »** (virgule APRÈS le code postal — le format majoritaire) donnaient `afterZip = ", Ville, France"` → `split[0]` = chaîne **vide** → repli sur le défaut `'Paris'` (ligne 224 `glsSafe(parsed.city || 'Paris', 40) || 'Paris'`). **FIX** : on prend le **1er segment NON vide** et on ignore un segment « France » : `const segments = afterZip.split(/[,\n]/).map(s=>s.trim()).filter(Boolean); let city = (segments.find(s=>!/^france$/i.test(s))||'').replace(/,?\s*France\s*$/i,'').trim();`. Testé node 8/8 (Le Plessis-Trévise, Lyon, Montreuil, Chevreuse, Saint-Rémy, Créteil + Paris quand c'est vraiment Paris) + **dryRun réel #1310** → `City:"Carcassonne"` (avant : Paris). Déployé `gls-create-shipment`. Pas de bump SW (Edge Function seule). ⚠️ Les étiquettes **déjà émises** avant ce fix sont chez GLS (ville Paris figée, irrécupérables) → Borhen doit vérifier les colis province récents non encore livrés ; les **nouvelles** étiquettes seront correctes.

## État au 26/06/2026 — SMS confirmation de commande = AUTOMATISÉ côté serveur (fiable)
- **Constat Borhen : « est-ce que le client reçoit bien une confirmation par SMS quand j'enregistre une commande ? » → NON.** Vérifié en base : sur les **10 dernières commandes** (RANOU/GLS, tél valide, 24→26/06), **0 SMS `confirmation`** ; le dernier datait du 18/06 (mes tests Karim B). Cause : le SMS confirmation v7.5.73 était déclenché **par le front** (`saveCmd`→`sendCmdSmsAsync('confirmation')`, setTimeout 1500ms) → fragile (cache navigateur ancien — l'app de Borhen ne se mettait pas à jour, cf. saga Dembélé v7.5.94 ; ou app fermée avant l'envoi). Les SMS veille/départ marchent eux car **serveur** (cron `sms-auto`). **FIX : nouvelle Edge Function `sms-confirmation`** (`supabase/functions/sms-confirmation/index.ts`) = scanne `commandes` `created_at >= now - fenetreMin` (défaut 30), exclut **#SAV** + **GLS-avec-tracking** (l'expédition gère) + déjà confirmé, et délègue à `send-cmd-sms {type:'confirmation'}` (qui gère tél invalide + dédup `sms_envoyes`). La **fenêtre temporelle exclut de fait les saisies rétroactives** (created_at passé) et évite tout spam des anciennes commandes. Body `{dryRun?, fenetreMin?}`. **Cron `sms-confirmation-10min`** `*/10 5-20 * * *` UTC (= **7h–22h50 Paris** été, toutes les 10 min). Testé dryRun (seule #1340 récente éligible, anciennes ignorées) + **1er envoi réel OK** → #1340 Mme Sylvanise « envoyé ». ⚠️ Le déclencheur **front** (saveCmd) est conservé → ceinture+bretelles : si le front envoie, le serveur saute (dédup) ; si le front rate (cache), le serveur prend le relais. ⚠️ ~1 crédit OVH/commande → surveiller le solde. Pas de bump SW (aucune modif HTML).

## État au 23/06/2026 — NOUVEAU : rapport de santé automatique par SMS (gardien)
- **Demande Borhen : recevoir 2 SMS bilan/jour confirmant que tout marche (tournée, SMS veille/départ, optimisation, commandes, approche, crédits).** Choix Borhen : **matin ET soir, détaillé**. Nouvelle Edge Function **`rapport-sante`** (`supabase/functions/rapport-sante/index.ts`) + helper **`creditsOVH()`** ajouté à `_shared/ovh-sms.ts` (lit `creditsLeft` via GET `/sms/{service}`). Body `{phase:'soir'|'matin', force?, dryRun?}`. **Contrôles** (tout en base, service role) : (1) tournée cible RANOU [demain si soir / aujourd'hui si matin] = nb clients + rentabilité (seuil 13) ; (2) SMS `veille` (soir) / `depart` (matin) envoyés parmi les clients à tél valide → ratio X/Y + warn si manquants ; (3) créneaux horaires détectés dans les notes (détecteur `optimDetecterCreneau` **porté en TS**, v7.5.91) + le plus serré affiché ; (4) commandes prises sur 24h (hors SAV) ; (5) SMS `proche` du jour ; (6) échecs SMS du jour (warn si >0) ; (7) crédits OVH (warn si <100). Message ≤2 segments, sans accents (via `sansAccents`), statut final `=> TOUT OK` ou `=> A VOIR: ...`. **Dédupe** : 1 rapport/phase/jour via `sms_historique` type `rapport-soir`/`rapport-matin` (sauf `force`). Destinataire = `RAPPORT_TEL` ou `+33744289321` en dur. **2 crons pg_cron** créés (jobid 6/7) : `rapport-sante-matin` `30 5 * * *` UTC = **7h30 Paris** (après le départ 7h), `rapport-sante-soir` `30 18 * * *` UTC = **20h30 Paris** (après la veille 19h). Testé en dryRun (soir+matin) + 1 vrai SMS matin envoyé à Borhen (success, « 1er Mokrane 08:45 », OVH 970 cr.). ⚠️ Heures calées sur l'**été (UTC+2)** comme les crons existants → en hiver (UTC+1) décalent à 8h30/21h30 (à réaligner au changement d'heure si gênant). ⚠️ Coût ~2 crédits/jour (~4 avec matin+soir) → surveille le solde OVH (le rapport lui-même l'affiche). Pas de bump SW (aucune modif HTML).

## État au 23/06/2026 (version v7.5.91) — Optimisation détecte « à 8h45 », « 14h-16h », heure cible
- **Constat Borhen : l'optimisation ne prenait pas en compte l'impératif « à 8h45 » (Mr ACHIR #1315, tournée mardi T-30) — « rien n'est pris en compte ».** Diagnostic (test node sur les vraies notes de T-30) : `optimDetecterCreneau` (~ligne 12224) détectait bien « avant midi/12h », « avant 15h », « entre 13h-16h », « apres 14h », mais **PAS** : (a) **« à 8h45 »** / « à 9h » / « pour 10h » / « livraison 8h45 » = **heure cible directe** sans mot-clé, (b) **« 14h - 16h »** = **plage écrite sans « entre/de »**, (c) une **heure isolée** (« 8h45 »). Ces notes → `imperatif=false` → client traité comme flexible → jamais remonté. **FIX (v7.5.91)** : 3 règles ajoutées au détecteur (avant le `return null`) : **mR** plage `Hh (-|/|a|au|et) Hh` → début+fin ; **mC** `(a|pour|vers|livr…) + heure` → échéance (`creneauFin`) ; **heure isolée** unique et **plausible (06:00–21:00)** → échéance (la borne évite « appel 1h avant » = 01:00). Testé node 16/16 : 11 vrais créneaux OK, 5 faux positifs ignorés (« code 4521 », « 2eme etage », « appeler avant de livrer », « appel 1h avant », « digicode B12 »). **Simulation ordre sur T-30** : ACHIR « à 8h45 » (échéance 525 min = la plus serrée) passe **1er**, puis BENCHEIKH/Traoré (avant midi), puis Bouju (14h-16h). Marche dans les **2 optimiseurs** (page + bouton fiche) car tous deux passent par `optimResoudreCreneau`→`optimDetecterCreneau`. Parse OK. Bump SW v7.5.91. ⚠️ Borhen doit **recharger l'app** puis relancer « Optimiser ».

## État au 21/06/2026 (version v7.5.90) — Report = renvoi des SMS veille/départ
- **Bug trouvé en faisant l'état des lieux des notifications client (demande Borhen).** Constat : Borhen a **reporté à mardi 23/06** les clients de la tournée du 21/06 qui n'a pas tourné. Les 2 commandes reportées (#1307 BENCHEIKH, #1313 Bouju) avaient déjà reçu **veille + départ** (la veille 20/06 19h et le matin 21/06 5h, pour une tournée qui n'a pas eu lieu) et gardaient `sms_envoyes=[veille,depart]`. **Conséquence** : la dédupe serveur (`commandes.sms_envoyes`) aurait **bloqué** le renvoi du SMS veille (lundi 22/06 19h) ET départ (mardi 23/06 7h) pour leur **nouvelle** date → les 2 clients **silencieux** pour mardi. Cause racine : un **report change `date_livraison` mais ne réinitialise pas `sms_envoyes`**. **FIX (v7.5.90)** : nouvelle fonction **`resetSmsTourneeFlags(cmdId)`** (~après `sbSaveCommande`, ~ligne 3025) = GET `sms_envoyes` → retire les types **liés à la tournée** (`veille/depart/route/proche`) → PATCH (conserve `confirmation`/`expedition` qui sont liés à la commande, pas au jour). Appelée dans **`saveCmd`** (branche édition) **quand `date_livraison` change** (`_ancienneDateLiv` mémorisé avant la maj). Contenu : aucune modif de `CMD_SELECT_COLS`/mappings/`sbSaveCommande` (la fonction fait son propre round-trip REST) → **zéro risque sur les calculs financiers**. **Données corrigées à la main** : #1307 et #1313 → `sms_envoyes=[]` (donc mardi : veille lundi soir + départ mardi matin repartiront via le cron `sms-auto`). Parse OK. Bump SW v7.5.90.
- **État des lieux notifications (vérifié en base, historique 05→21/06) :** ✅ **partent réellement** = `depart` (27, dont 21/06 5h), `veille` (23, avec lien confirmation), `expedition` GLS (8), `confirmation` prise de commande (2 — uniquement mes tests Karim B du 18/06, aucune vraie nouvelle commande depuis). ❌ **`proche` (approche ~30 min) = 0 envoi depuis le 05/06** : aujourd'hui c'est NORMAL (pas de tournée) mais **jamais parti** historiquement → le vrai test sera **mardi 23/06** (nécessite livreur app ≥v7.5.84 + GPS actif + au 1er plan + passage <30 min d'un client). ⚠️ **`livre` (confirmation à la signature) non tracé** dans `sms_historique` (envoyé via `brevoSendSMS`/`send-sms` sans type loggé) → impossible à prouver, trou de traçabilité (pas forcément une panne).

## État au 21/06/2026 (version v7.5.89) — Clic client dans la fiche tournée
- **Demande Borhen : cliquer un client dans la fiche tournée pour changer son statut ou modifier sa fiche.** Avant, les `.stop-card` de `afficherTournee` (~17428) n'étaient pas cliquables. Ajout : `onclick="ouvrirCmd(s.cmdId)"` + `cursor:pointer` sur chaque carte (si `s.cmdId`) → ouvre le panneau commande qui contient déjà les boutons de statut (`chgStatut`, qui synchronise la tournée v7.5.87/88) ET le bouton Modifier. Parse OK. Bump SW v7.5.89.

## État au 21/06/2026 (version v7.5.88) — Commande annulée = retirée de la tournée
- **Suite v7.5.87 : Borhen veut qu'une commande annulée SORTE de la tournée** (pas juste marquée barrée). `reconcilierTournees` PASSE 1 : `if (cmd.statut === 'annulé') { change = true; return; }` → le stop non livré est **retiré** (au lieu de `s.statut='annulé'` gardé). Un stop déjà `livré` reste (historique). Testé (node) : annulée→retirée, en-attente gardée, livré gardé. Parse OK. Bump SW v7.5.88.

## État au 21/06/2026 (version v7.5.87) — Synchro Commandes ↔ Tournées complète
- **Demande Borhen : les rubriques Commandes et Tournées doivent communiquer dans les 2 sens.** Avant : changer une commande (annuler, reporter, changer la date) ne se répercutait PAS sur la tournée — le client annulé restait dans la tournée, un report ne déplaçait pas le client. Cause : `chgStatut` (boutons statut du panneau commande, ~7484) ne touchait pas les tournées, et `autoPlanifierTournee` ne savait que **ajouter** (jamais retirer un annulé/inéligible). **Refonte en UNE seule synchro** : `reconcilierTournees()` (~ligne 8271) fait désormais **PASSE 1 (nettoyage)** — pour chaque stop : commande livrée→stop livré (gardé), commande **annulée→stop marqué annulé** (gardé, exclu du compteur 13), commande **GLS/SAV/sans date→stop retiré**, commande dont la **date a changé→stop retiré** (réajouté à la bonne date) ; stops `livré` et sans `cmdId` préservés. **PASSE 2 (ajout)** — toute commande éligible absente est rangée (création tournée si besoin). Helper **`cmdEligibleTournee(cmd)`** centralise la règle. **`autoPlanifierTournee` délègue maintenant à `reconcilierTournees`** (plus de double logique divergente). Appelée à 3 endroits : chargement (`sbLoadAll`, gardé), enregistrement (`saveCmd`), **changement de statut (`chgStatut`)**. Idempotent (0 écriture si cohérent). Testé (node) : annulé→stop annulé ; reporté 25/06→déplacé en T-27 ; GLS→retiré ; nouvelle→ajoutée ; livré admin→stop livré ; 2e passage 0 doublon. Parse OK. Bump SW v7.5.87. ⚠️ La synchro tourne dans le navigateur de chaque poste (chargement + actions) ; `sbSaveTournee` upsert. Au prochain rechargement de Borhen, les incohérences EXISTANTES (annulés/reportés encore dans les tournées) sont nettoyées automatiquement.

## État au 20/06/2026 (version v7.5.84) — FIX SMS d'approche (jamais déclenché)
- **Constat (état des lieux SMS demandé par Borhen) : le SMS « proche » (le chauffeur arrive ~30 min) n'est JAMAIS parti — 0 dans `sms_historique`** (vs veille/départ/expédition/confirmation qui marchent). GPS livreur **actif** (positions RANOU en base), crédits OVH **= 19** (critique). **Cause racine** : DEUX watchers GPS séparés — `gpsDemarrerReel`→`gpsEnvoyerPosition` (envoie les positions, **actif**) et `geofenceDemarrer`→`geofenceTraiterPosition`→`verifierProximiteClients` (vérif proximité + SMS, **souvent inactif** pendant la tournée). La vérif de proximité n'était branchée QUE sur le watcher géofence → jamais appelée → 0 SMS. **Fix** : appel de **`verifierProximiteClients(lat, lng)` ajouté dans `gpsEnvoyerPosition`** (~ligne 16298) = le flux GPS confirmé actif (positions toutes les 30 s). Les throttles internes (90 s/client OSRM, dédup 1 h, géocodage 1 stop/passage) évitent l'abus. + date du jour passée en **heure LOCALE** dans `verifierProximiteClients` (avant `toISOString` UTC). Testé (node) : parse OK ; à la vraie position de RANOU (Aubervilliers) le SMS se déclenche pour Abazzar (2 min), Traoré (6 min), Cadet (14 min), Mukelenge (27 min), pas Versailles (52 min). Bump SW v7.5.84. ⚠️ Nécessite que le LIVREUR recharge (≥v7.5.84) + GPS actif + app au 1er plan. ⚠️⚠️ **1 SMS proche/client = 1 crédit OVH → avec 19 crédits, recharger OVH d'urgence** sinon coupure.

## État au 20/06/2026 (version v7.5.83) — FIX URGENT vue livreur (tournée du jour)
- **Bug urgent : le livreur ne voyait pas sa tournée du jour** (la vue affichait Isabela #1286 du 14/07 au lieu de T-25 du 20/06). Cause : `renderLivreur` (~8544) sélectionnait la tournée du livreur en triant par **date DÉCROISSANTE** puis prenait la 1ère non terminée → la date la **plus lointaine**. Sans tournées futures c'était OK ; mais le rattrapage v7.5.82 a créé les tournées futures (T-26 21/06, T-27 25/06, T-28 14/07, T-29 30/06) → la vue tombait sur la plus lointaine. Fix : helper **`lvChoisirTournee(liste)`** (~ligne 8521, dans `renderLivreur`) = tournée du JOUR (`date === lvToday`, calculé en heure LOCALE) en priorité, sinon la **prochaine à venir** (date la plus proche ≥ aujourd'hui), sinon la plus récente active. Branché dans les 2 branches (livreur ET admin par défaut). Le **SAS chargement** (~8897) lit `currentLvTourneeId` → corrigé du même coup. Testé (node) : aujourd'hui→T-25 ; sans tournée du jour→T-26 (prochaine) ; que du passé→plus récente active. Parse OK. Bump SW v7.5.83. Données vérifiées : T-25 (20/06) intacte 13 stops ; le rattrapage n'a PAS créé de doublon (1 seule tournée par date).

## État au 19/06/2026 (version v7.5.82) — Rattrapage auto des tournées (anti-trou)
- **Demande Borhen : fiabiliser l'auto-planification (v7.5.72) — une commande dimanche (#1307) saisie pendant la coupure réseau du jour n'avait pas créé sa tournée** (la création, faite en mémoire, perdue au rechargement car Supabase injoignable). **Donnée corrigée** : tournée **T-26** (21/06, RANOU, #1307 Mme BENCHEIKH) créée à la main. **Fix robustesse** : nouvelle fonction **`reconcilierTournees()`** (~ligne 8258, après `autoPlanifierTournee`) appelée à la fin de **`sbLoadAll`** (chemin succès), **gardée par `if(!cmdsRes.error && !tourRes.error)`** (ne tourne QUE si les 2 chargements serveur ont réussi → sinon `tournees` incomplet = risque de doublons). Logique : pour chaque commande **IDF/RANOU, datée, non livrée/annulée, non #SAV, non GLS** qui n'est **dans AUCUNE tournée** (par `cmdId`), find-or-create la tournée RANOU de sa date (via `nextTourneeId()`) et ajoute le stop. **N'ajoute QUE les absentes** (ne déplace/ne touche pas les assignations existantes — contrairement à `autoPlanifierTournee`). **Idempotent** : si tout est déjà rangé, 0 écriture. Testé (node) : #1307+#1308 dimanche → 1 seule T-26 ; GLS/SAV/livré/sans-date ignorés ; 2e passage = 0 sauvegarde (pas de doublon). Parse OK. Bump SW v7.5.82. ⚠️ Tourne dans le navigateur de chaque poste au chargement → un poste ouvert récupère les trous ; le `sbSaveTournee` est upsert (pas d'écrasement). Petite fenêtre de course si 2 postes rattrapent la même date au même instant (atténué : le 2e voit la tournée du 1er via polling avant son prochain load).

## État au 19/06/2026 (version v7.5.81) — Les 2 optimiseurs lisent les créneaux pareil
- **FIX (constat Borhen : l'ordre diffère entre la page « Optimiser tournée » et le bouton « Optimiser » de la fiche tournée).** Cause racine : les deux construisaient les fenêtres horaires `infos{ready,due}` **différemment**. `optimCalculer` (page) faisait `if (s.imperatif && s.creneauDebut)` → exigeait une heure de **début** → les créneaux « avant 9h30 »/« avant midi » (qui n'ont QUE `creneauFin`) étaient **ignorés** → le client traité comme flexible → ordre incohérent (capture Borhen : Virginie 14-16h en 1er, Cédric 9h30 en **dernier**). `optimiserTourneeRapide` (bouton) posait `due` depuis `creneauFin` indépendamment → correct. **Correctif** : helper unique **`optimResoudreCreneau(s)`** (~ligne 12149, après `optimDetecterCreneau`) = champs structurés `imperatif/creneauDebut/creneauFin` en priorité, sinon détection note `s.instr` ; renvoie `{imperatif, creneauDebut, creneauFin, ready, due}`. **Branché dans les DEUX** (`optimCalculer` ~12324 ET `optimiserTourneeRapide` ~12735) → impossible qu'ils divergent à nouveau. Simulé (node) sur la tournée du 20/06 : AVANT page = `Virginie>Cadet>Annour>Laguenaoui>CÉDRIC` ; APRÈS = `CÉDRIC>Cadet>Annour>Laguenaoui>Virginie` (Cédric 9h30 en 1er, comme le bouton). Parse OK. Bump SW v7.5.81. ℹ️ Le `s.imperatif && s.creneauDebut` restant (~12409, `optimCalculerETAs`) est correct : c'est l'attente d'ouverture de créneau (on n'attend que s'il y a une heure de début).

## État au 18/06/2026 (version v7.5.80) — Ensembles = virtuels, alertes sur le physique
- **Analyse « pourquoi du rouge partout sur l'inventaire » + fix voyant ensembles.** Constat : un ENSEMBLE a un `stock` propre **figé** (jamais décrémenté — ce sont ses `composants` matelas+sommier qui le sont, via `stkDeduireStock`, 19/23 ensembles configurés). Or `stkRender` basait le **voyant/alertes/KPI** sur ce `stock` figé → incohérent (ensemble « vert » alors que composants à 0, ou inversement) + double bruit. **Fix (point 1, demandé par Borhen)** : (a) **tableau** — chaque ligne utilise `eff = getStockDispo(p)` (= dispo composants pour un ensemble, stock normal sinon) pour le badge/barre/chiffre → voyant cohérent avec la réalité ; (b) **bannière d'alertes + KPI ruptures/alertes/dispo** → ne portent QUE sur les produits **physiques** (`estVirtuel(p)=composants.length>0` exclus) car un ensemble est virtuel et l'alerter en plus ferait doublon avec ses composants (« alertes partout »). Résultat vérifié sur données réelles : bannière = **9 alertes physiques utiles** (6 vrais articles à réappro : matelas 140×190=0, sommiers tapissiers=0, matelas 160×200=3, lits NICO 140×190=2 ; + 3 ensembles orphelins) au lieu d'un mélange incohérent. Parse OK. Bump SW v7.5.80.
- **⏳ À FAIRE (enregistré, demande Borhen « tu enregistres à faire ») :**
  - **Point 2 — 3 ensembles SANS composants** (donc comptés comme articles physiques → fausses ruptures) : `pr08` « Ensemble 160x200 Queen », `pr1780615649` « Ensemble 140×200 À Lattes », `pr06` « Ensemble 140x190 15cm À Lattes ». → soit leur **configurer les composants** (matelas + sommier seuls, comme les 19 autres, via le bouton 🔧 Composition), soit les **désactiver** si ce sont des doublons. Décision Borhen requise.
  - **Point 3 — process réappro** : quand Borhen réapprovisionne physiquement des matelas/sommiers/lits SEULS, mettre à jour leur compteur (bouton ±). Sinon ils tombent à 0 et alertent légitimement. Idéalement faire un **inventaire physique** une fois pour caler les compteurs composants à la réalité (plusieurs à 0 : matelas 140×190, sommiers tapissiers 90×190 et 140×190).
  - **Nettoyage doublons catalogue** : anciens produits `pr03–pr09` + versions récentes `pr177…` pour le même article → fusionner/désactiver (demande décisions Borhen).
  - **Mineurs non traités** (audit v7.5.79) : substitution composant à l'annulation, KPI « livrés ce mois » par nom, garde-fou ajustement « correction », course Realtime stock, `mx_stocks` clé morte.

## État au 18/06/2026 (version v7.5.79) — Inventaire stock fiabilisé (audit + 3 fix)
- **Audit du module Inventaire stock (à la demande de Borhen) → 3 correctifs critiques.** Constats : déduction/recrédit retrouvaient le produit **par nom approximatif** (ignorant `l.produitId` pourtant présent) → commandes site Shopify / produits renommés non décrémentés **en silence** ; garde anti double-déduction basé sur `stkMouvements` **volatil** (réinit. à 5 lignes démo à chaque chargement, jamais persisté) → double déduction après rechargement + recrédit d'annulation cassé ; bouton « Actualiser » = simple re-render mémoire.
  - **FIX 1 — déduction/recrédit par ID** : helper **`stkTrouverProduit(produitId, nomProduit)`** (~ligne 11146) = `produits.find(id===produitId)` d'abord, repli nom sinon. `stkDeduireStock.deduire(produitId, nom, qte)` et `stkReCrediterStock.recredit(produitId, nom, qte)` l'utilisent ; les appels passent `l.produitId`. ⚠️ Les commandes **site** dont `produitId` n'est pas un id catalogue (`_custom_…` ou null) retombent sur le nom — pour les fiabiliser à 100 % il faudrait mapper les produits Shopify→catalogue dans `shopify-sync` (chantier séparé).
  - **FIX 2 — flag durable `stock_deduit`** : nouvelle colonne DB `commandes.stock_deduit boolean DEFAULT false` (+ `CMD_SELECT_COLS`, 3 mappings `stockDeduit`, `sbSaveCommande`). `stkDeduireStock` : garde `if(cmd.stockDeduit) return;` puis `cmd.stockDeduit=true; sbSaveCommande(cmd)`. `stkReCrediterStock` : `if(!cmd.stockDeduit) return;` puis recrédit (ENSEMBLE → recrédite ses composants par id, miroir de la déduction) puis `cmd.stockDeduit=false; sbSaveCommande(cmd)`. Survit au rechargement et au polling. ⚠️ Commandes **déjà livrées avant ce déploiement** ont `stock_deduit=false` → une annulation rétroactive ne recréditera pas (comportement passé déjà non fiable ; le forward est correct).
  - **FIX 3 — `stkActualiser()`** (~ligne 11437) : recharge `sbLoadProduits()` puis `stkRender()` ; bouton « 🔄 Actualiser » (~1695) rebranché.
  - Testé (node) : parse OK ; `stkTrouverProduit` id prime sur nom, repli nom pour id Shopify inconnu, null sinon ; garde double-déduction OK (2e appel bloqué). Bump SW v7.5.79. **Points NON traités (hors scope des 3 fix)** : substitution de composant à l'annulation (recrédit nominal, pas le substitut), KPI « livrés ce mois » recompté par nom, ajustement « correction » sans garde-fou, course Realtime sur le stock (last-write sans comparaison `updated_at`), `mx_stocks` (clé morte).

## État au 18/06/2026 (version v7.5.78) — Impératif horaire bien VISIBLE sur la vue livreur
- **Demande Borhen : RANOU doit voir clairement l'impératif horaire sur son interface** (« les employés, faut que ça saute aux yeux »). Avant, la note `s.instr` (ex « IMPERATIF AVANT 9H30 ») n'apparaissait qu'en petit encadré orange discret (lv-card ~8615). Ajout : **gros bandeau ROUGE en haut de la carte** de chaque arrêt concerné dans la **Vue livreur** (`renderLivreur`, lv-list ~8552) : « ⏰ IMPÉRATIF HORAIRE : AVANT 9H30 » (font 900, fond #E53E3E, ombre). Helper **`lvCreneauLabel(s)`** (~ligne 8440, avant `renderLivreur`) = créneau depuis les champs structurés (`s.imperatif/creneauDebut/creneauFin` posés par l'optimisation) OU, à défaut, **détecté dans la note** via `optimDetecterCreneau(s.instr)` → visible **même sans avoir lancé l'optimisation**. Libellé : « AVANT HH:MM » / « CRÉNEAU HH:MM – HH:MM » / « À PARTIR DE HH:MM ». Masqué si arrêt déjà livré (`!done`). Testé (node) : Cédric→« AVANT 09:30 », Virginie→« CRÉNEAU 14:00 - 16:00 », Abazzar→10:30, Annour→12:00 ; rien pour « code 4521 »/sans note. Parse OK. Bump SW v7.5.78.

## État au 18/06/2026 (version v7.5.77) — Impératif le plus serré TOUJOURS en 1er
- **Règle Borhen : l'impératif horaire le plus serré (FIN de créneau la plus tôt) doit TOUJOURS passer en 1er** (sécurité, quitte à rouler un peu plus). Avant (v7.5.76), l'algo respectait le créneau mais pouvait servir l'impératif 9h30 en 2e (ex Cédric 9h12) si un autre stop matin était sur le chemin. Nouveau helper **`optimOrdonnerCreneaux(allIdx, depotIdx, endpointIdx, ms, md, infos, startMin, dureeStop)`** (~ligne 11990, après `optimAmeliorerCreneaux`) : identifie l'impératif à `due` minimale (tie-break `ready` mini), l'**épingle en position 1**, puis optimise le reste avec « départ » = ce stop forcé (heure de départ = arrivée + attente éventuelle + durée) via `optimAmeliorerCreneaux` (2 départs → meilleur). Si aucun impératif à échéance → optimisation normale. **Branché dans les 2 chemins** : `optimCalculer` (page Optimiser) ET `optimiserTourneeRapide` (bouton fiche tournée) — les blocs « 2 départs → meilleur » remplacés par un appel à `optimOrdonnerCreneaux`. **Simulé (node) sur la vraie tournée du 20/06** : Cédric (avant 9h30) passe **1er @8h44**, Abazzar (10h30)→9h45, Annour (midi)→10h17, Virginie (14h-16h)→14h00 — 0 retard. Parse OK. Bump SW v7.5.77.

## État au 18/06/2026 (version v7.5.76) — Le BON bouton "Optimiser" respecte les créneaux
- **FIX (re-test Borhen : l'impératif 9h30 ne remontait toujours pas).** Cause : **DEUX** chemins d'optimisation. Borhen clique le bouton **« 🚀 Optimiser » de la fiche tournée** (`btn-td-optimiser` ~ligne 785) → **`optimiserTourneeRapide()`** (~12550), une fonction **séparée** qui faisait NN+2-opt sur les **durées seules** (étape 4) → ignorait totalement les créneaux. La page « Optimiser tournée » (menu gauche, `optimCharger`/`optimCalculer`) avait bien la nouvelle logique v7.5.74/75, mais ce n'est pas le bouton utilisé. Correctif : **porté la détection des notes + l'ordonnancement avec fenêtres horaires dans `optimiserTourneeRapide`** — `optimDetecterCreneau` sur chaque stop (`s.instr`/`cmd.instr`) → `infosR{ready,due}` ; remplacé `optimNearestNeighbor`+`optim2opt` par 2 départs (NN + tri échéance) → `optimAmeliorerCreneaux` (coût `optimCoutCreneaux`, départ 08:00 / 15 min par défaut car ce bouton n'a pas de champ heure) ; les créneaux détectés sont **persistés** sur les stops (`imperatif/creneauDebut/creneauFin`) pour l'affichage ; toast « N créneaux détectés ». **Simulé (node) sur la vraie tournée du 20/06** (12 clients, 4 créneaux) : tous respectés, 0 retard — Cédric (avant 9h30) servi à **9h12**, Virginie (14h-16h) à 14h00, etc. Parse OK. Bump SW v7.5.76. ℹ️ L'algo sert le plus efficace tant que les créneaux sont tenus → un impératif « avant 9h30 » peut être servi 2e (ex 9h12) si un autre stop matin est sur le chemin ; le créneau **est** respecté (les 2 chemins partagent désormais `optimCoutCreneaux`/`optimAmeliorerCreneaux`). Option possible si Borhen veut le plus serré STRICTEMENT en 1er (testé : un biais « marge » ne suffit pas, dominé par le créneau après-midi → faudrait une contrainte d'ordre dédiée).

## État au 18/06/2026 (version v7.5.75) — L'optimisation lit les créneaux dans les NOTES
- **FIX (suite test Borhen) : l'optimiseur ne respectait pas un créneau écrit en TEXTE dans la note de commande** (ex #1277 Cédric « IMPERATIF SAMEDI AVANT 9H30 » placé en 3e au lieu d'être servi tôt). Cause : v7.5.74 lit les champs structurés `imperatif`/`creneauDebut`/`creneauFin` de la page Optimiser, que Borhen ne remplit pas — il écrit la contrainte en **texte libre** dans `instr`. Correctif : helper **`optimDetecterCreneau(texte)`** (~ligne 12011) qui parse la note et en déduit le créneau : « avant 9h30 »/« avant midi » → `creneauFin` ; « entre 14h-16h »/« de 9h a 11h » → début+fin ; « apres 14h »/« a partir de 15h30 »/« apres midi » → `creneauDebut` ; « imperatif/creneau/RDV + heure » → deadline. Insensible casse/accents. **Appelé dans `optimCharger`** : chaque stop lit `s.instr` (ou `cmd.instr`) → `imperatif/creneauDebut/creneauFin` auto-remplis, **sans cocher « Impératif » à la main**. Toast récap « N créneau(x) détecté(s) ». Testé (node) sur les 4 vraies notes du 20/06 (« IMPERATIF...AVANT 9H30 »→09:30 ✓, « entre 14h-16h »→14:00-16:00 ✓, « avant 10h30 »→10:30 ✓, « AVANT MIDI »→12:00 ✓) + 0 faux positif sur « appeler avant de livrer »/« code 4521 »/« 2eme etage ». Parse OK. Bump SW v7.5.75. ⚙️ Le respect du créneau lui-même est assuré par l'algo v7.5.74 (pénalité si dépassement de `creneauFin`). ⚠️ Borhen doit **recharger l'app** puis relancer l'optimisation pour voir l'effet.

## État au 18/06/2026 (version v7.5.74) — Optimisation tournée AVEC fenêtres horaires
- **Refonte du cœur de l'optimisation (demande Borhen) : la tournée se classe en respectant les créneaux horaires (début ET fin) tout en gardant l'optimisation km/temps, le départ entrepôt et l'arrivée domicile livreur.** AVANT (v7.5.50) : on optimisait SEULEMENT les flexibles (NN+2-opt) puis on **injectait** les impératifs par heure de **début** — ça ne contrôlait **pas la FIN** du créneau (livraison possible trop tard) et cassait l'optimalité. MAINTENANT : ordonnancement **unifié** sur TOUS les stops via 2 nouvelles fonctions (~ligne 11937) : **`optimCoutCreneaux(...)`** = coût = trajet (matriceScore : km/temps/trafic, **inchangé**) **+ grosse pénalité (LAMBDA=1000)** si arrivée **après `creneauFin`** ; planning en minutes réelles (matriceDuree) avec **attente** si arrivée avant `creneauDebut`. **`optimAmeliorerCreneaux(...)`** = recherche locale **2-opt + or-opt** minimisant ce coût. Dans `optimCalculer` (étapes 3-6 réécrites) : 2 ordres initiaux (plus-proche-voisin **et** tri par échéance de créneau) → amélioration locale → on garde le meilleur. `infos[mIdx]={ready,due}` en minutes (null = flexible). Départ `depotIdx` fixe, arrivée `endpointIdx` (domicile livreur) fixe. **Testé (node) sur 3 scénarios** : sans créneau → ordre trajet optimal ; impératif au bout avec créneau serré → servi **en premier malgré le détour, 0 retard** ; créneau large → garde l'ordre optimal ET respecte la fenêtre. JS parse OK (0 erreur, 751 K). ✅ **SMS d'approche client non impacté** (basé GPS proximité, indépendant de l'ordre). Bump SW v7.5.74. ℹ️ L'ancien `optim2opt` reste défini (non utilisé par le nouveau flux) ; l'option « Impératifs en priorité » du menu est désormais redondante (les créneaux sont toujours respectés). ⚠️ Si un créneau est **physiquement intenable**, l'algo choisit l'ordre le **moins en retard** (pénalité minimale) — le KPI global « fin dans les temps/retard » le signale ; une alerte par-créneau dédiée reste un ajout possible (Borhen a dit non pour l'instant).

## État au 18/06/2026 (version v7.5.73) — SMS de confirmation à la prise de commande
- **NOUVEAU (demande Borhen) : SMS de confirmation automatique à chaque NOUVELLE commande**, pour rassurer/fidéliser le client (« qu'il n'aille pas voir ailleurs ») : commande prise en charge, en cours d'acheminement, livrée à la date indiquée. Nouveau type **`confirmation`** dans `send-cmd-sms` (template ≤144 car = **1 crédit OVH** ; avec date → « sera livree le JJ/MM », sans date → message générique « nous vous communiquerons la date »). Déclenché côté front dans **`saveCmd`** (branche nouvelle commande uniquement) via `sendCmdSmsAsync(id, 'confirmation')`. **EXCLUS** : SAV (`#SAV`), saisie **rétroactive** (`_retroactiveCreatedAt`), tél absent/invalide, et **GLS avec tracking déjà saisi** (le SMS `expedition` part déjà → pas de doublon). **Dédupe serveur** via `sms_envoyes` (type `confirmation`) = jamais 2 fois la même. Déployé `send-cmd-sms` + bump SW v7.5.73. SMS mesuré ≤144 car même « Jean-Baptiste ». ⚠️ **Coût** : 1 crédit par commande → avec ~45 crédits OVH restants, **Borhen DOIT recharger OVH** rapidement (sinon les SMS s'arrêtent). ℹ️ Concerne toutes les nouvelles commandes (IDF **et** province), pas seulement l'IDF — conforme à « chaque nouvelle commande ».

## État au 18/06/2026 (version v7.5.72) — Auto-planification des tournées par date
- **NOUVEAU module (demande Borhen) : remplissage automatique des tournées par date de livraison + signal de rentabilité.** Quand Borhen enregistre une commande **à livrer nous-mêmes (Île-de-France / RANOU)** avec une **date de livraison**, elle se range **automatiquement** dans **la tournée de cette date** (créée si elle n'existe pas encore). Les commandes s'**accumulent** par date ; la **carte de tournée** affiche le **compteur de clients** et passe au **VERT** dès **`TOURNEE_SEUIL_RENTABLE` (=13) clients** = « rentable, prêt à envoyer le livreur ». C'est un outil de **planification interne admin**, pas un blocage livreur. Implémentation : helper **`autoPlanifierTournee(cmd)`** (~ligne 8180, à côté de `nextTourneeId`) appelé en fin de **`saveCmd`** (nouvelle commande ET édition). Logique calquée sur `suiviValiderAjoutTournee` : find-or-create de la tournée RANOU de la date (via `nextTourneeId()` = max+1, anti-écrasement), ajout/MAJ du stop **sans doublon** (idempotent par `cmdId`), **déplacement** si la date change (retrait des autres tournées en préservant les stops `livré`), `sbSaveTournee`. **Exclus** : commandes **GLS** (province, expédiées), **#SAV** (gérés à la main), **livré/annulé**. Carte verte dans `renderTournees` (compteur `clients`=stops non annulés ; bordure verte + badge « ✅ X clients · prêt à envoyer » si `préparée` ET ≥13 ; sinon « 👥 X/13 — en remplissage »). Testé (node) : 3 IDF du 20 → 1 tournée 3 stops ; GLS/SAV ignorés ; re-save sans doublon ; changement de date déplace la commande. JS parse OK. Bump SW v7.5.72. ⚠️ Ne couvre que la **saisie manuelle** (`saveCmd`) — les imports site IDF (rares, la plupart sont GLS/province) ne sont pas auto-planifiés par `shopify-sync` (extension possible). ⚠️ Petite fenêtre de course si 2 postes créent la tournée d'une même date au même instant (2 tournées pour 1 date) — atténué par le polling. ⚠️ Les commandes IDF **déjà en attente avant ce déploiement** n'apparaîtront dans une tournée qu'au prochain ré-enregistrement (backfill possible sur demande).

## État au 18/06/2026 — shopify-sync v6.2 (transporteur auto IDF/province)
- **Choix automatique du transporteur à l'import site (règle Borhen).** Une commande site **hors Île-de-France** (province ou étranger) est désormais classée **transporteur GLS** automatiquement ; **IDF** (dép. **75/77/78/91/92/93/94/95**) reste **RANOU** (livreur interne). Helper `transporteurPour(addr)` dans `shopify-sync` (lit `shipping_address.zip` → 2 premiers chiffres ; `country_code` ≠ FR → GLS d'office). Avant, le champ `transporteur` n'était pas mappé à l'import → tout retombait sur le défaut RANOU. **Donnée corrigée** : #1029 (Simon, Saint-Rémy-de-Provence 13210, en-attente) repassée RANOU→GLS. ℹ️ #1019 (Apremont 60, province, RANOU) laissée car **déjà livrée**. Déployé `shopify-sync` v6.2 (Edge Function seule, pas de bump SW).

## État au 18/06/2026 (version v7.5.71) — shopify-sync v6.1 + champ frais_port
- **FIX CRITIQUE — import Shopify écrasait potentiellement des commandes + montant gonflé.** Deux bugs trouvés en vérifiant la remontée des commandes du site Maxiconfort.fr : **(1) ID en collision** — `mapShopifyToCmd` mettait `id = o.name` (le n° de commande **Shopify**, ex `#1030`) comme id app. Or Shopify a sa **propre** numérotation (≈#1030) qui tombe dans la plage des commandes app (saisies LeBonCoin) ; l'`upsert onConflict:'id'` aurait **écrasé** la commande app portant ce numéro. Pas encore déclenché car les saisies manuelles démarrent à **#1047** et Shopify était à #1030 — **collision imminente** (~17 commandes site). Correctif : helper **`maxNumeroCommande()`** → l'import prend le **prochain n° app libre (max+1)** (comme `genererCmdId`) ; `ref_marketplace`=ID Shopify reste la clé de dédup ; n° Shopify conservé dans `instr` (« Commande site #1030 »). **(2) Montant — décision Borhen = COMPTER les frais de port dans le CA.** `prix = current_total_price` incluait déjà le port, MAIS de façon **fragile** : le port était dans `prix` sans être dans les `lignes`, donc **rouvrir/éditer** la commande recalculait `prix` depuis les lignes (produits seuls) et **perdait le port** (cause réelle de #1030 retombé à 139 au lieu de 168,90). Correctif propre : **nouveau champ DB `frais_port`** (`ALTER TABLE commandes ADD COLUMN frais_port numeric DEFAULT 0`) + ajouté à `CMD_SELECT_COLS`, aux **3 mappings** (`fraisPort`), à `sbSaveCommande`, à `saveCmd` (`prix = (produits − remise) + fraisPort`, port préservé à l'édition car le formulaire n'a pas de champ port), à `recalcTotal` (aperçu) et au **panneau commande** (ligne « 🚚 Frais de livraison +X € »). L'import met `frais_port = total_shipping`, `prix = subtotal_price + port`, remise commande en remise globale `type:'eur'`. Le port **n'est PAS** mis dans les lignes (sinon il polluerait chargement/stock/facture). Résultat **edit-safe** : rouvrir #1029 → (538−26,90)+29,90 = 541 ✓. Déployé (`shopify-sync` v6.1) + bump SW v7.5.71. JS vérifié (parse OK, 751 K chars sans erreur). **Données corrigées** : #1029 (Simon) → 541 (port 29,90) ; #1030 (Antonin) → 168,90 (port 29,90). ⚠️ Imports site **déjà livrés** (#1009-#1028) gardent leur ancien montant (CA passé non retouché — Borhen a dit « laisse comme ça »). ⚠️ Petite fenêtre de course (~10 s) si admin/collab/import créent une commande au même instant (même `max+1`) — risque faible, bien moindre que l'écrasement systématique d'avant.

## État au 18/06/2026 (version v7.5.70) — Anti-homonyme (rattachement stop↔commande)
- **FIX — un stop ne se rattache plus au mauvais client homonyme.** Plusieurs endroits rattachaient un stop à une commande par **repli sur le nom** (`commandes.find(c => c.client === s.client)`) quand le stop n'avait pas de `cmdId` (stops legacy) → prenait le **1er homonyme**. Le plus grave : le **polling** (~3805) **écrivait** `s.prix/s.paie/s.stpaie` du mauvais homonyme sur le stop → caisse/tournée faussées en silence ; idem la mise à jour de mode de paiement (~9181, écrit `cmd.paie`). Correctif : helper **`findCmdForStop(s, filtre)`** (~ligne 2887, à côté de `findStopForCmd`) = priorité au `cmdId` (unique) ; repli par nom **uniquement si le nom est UNIQUE en base** (sinon renvoie `null` = on ne devine pas) ; `filtre` optionnel (ex statut livré). Appliqué à **7 sites** (polling 3805, maj paiement 9181, bottom-sheet paiement 8987, progression chargement 3459, checklist 8460, prix affiché 8495, liste chargement 8804) + le repli caisse par nom+livreur (~9898) rendu non-ambigu (filter + `length===1`). Testé navigateur : 2 « Mr Martin » sans cmdId → `null` (avant : 1er homonyme) ; stop avec cmdId → match strict ; nom unique → match ; filtre OK ; script parse. ℹ️ Laissé tel quel (risque faible, sémantique différente) : `t.stops.find(o=>o.client===s.client)` (~12326, recherche de stop pour réordonnancement, repli sur `s.idx`).

## État au 18/06/2026 (version v7.5.69) — Décision « Encaissé » = B
- **Suite du check-up : décision Borhen sur le sens d'« Encaissé » = option B (total des ventes réglées, prépayé inclus).** Aucun changement de fond nécessaire (le prépayé était déjà compté). **Fix v7.5.69** : `especesRemises` utilise `getMontantEspecesCmd` (part espèces des Mixte) → cohérent avec `especesCollectees`. Vérification données : **0 commande « Partiel », 2 Mixte (payées plein)** → le « bug » Mixte-partiel est théorique, non corrigé volontairement (voir backlog v7.5.68 ci-dessous). Testé navigateur (script parse, helper espèces OK : Mixte→part espèces, Espèces→plein, Non payé→0).

## État au 18/06/2026 (version v7.5.68) — Check-up santé complet
- **Audit de fond en comble (5 axes : sécurité, finances/caisse, intégrité données, SMS/Edge Functions, bugs JS).** Verdict global : app bien construite, bug tournées v7.5.66 bien refermé partout, migration Brevo→OVH propre. 3 quick-wins corrigés ce jour ; le reste documenté ci-dessous comme backlog priorisé.
- **FIX (v7.5.68) — ID de commande robuste = `genererCmdId()` (jumeau du bug tournées)** : le n° d'une nouvelle commande venait d'un **compteur figé** (`nextCmdId`) calculé **une seule fois au chargement** (~ligne 3568) puis `'#'+nextCmdId++` (~7121). Si une 2e session (collaboratrice CV) créait une commande entre-temps, le compteur local ne le voyait pas (le polling ajoute la commande dans `commandes[]` mais **ne recalcule jamais `nextCmdId`**) → la nouvelle commande pouvait reprendre un n° existant → **UPSERT merge-duplicates ÉCRASE la commande de l'autre poste** (même mécanisme que v7.5.66). Correctif : helper **`genererCmdId()`** (~ligne 8124, calque de `nextTourneeId()`) = `max(numéros existants dans commandes)+1`, calculé **au moment de la création**, ignore les `#SAV…` (NaN→0), filet anti-régression. Testé navigateur : `#1250/#1253/#SAV/#1251` → `#1254` puis `#1255` ; et surtout une commande `#1300` injectée « par un autre poste » (sans toucher nextCmdId) → ID suivant `#1301`, **0 collision**.
- **FIX (v7.5.68) — perte silencieuse des sauvegardes secondaires** : `sbSaveDepense/Produit/Vehicule/Livreur` (+ `Entretien`) ne faisaient qu'un `console.warn` si le POST échouait **en étant en ligne** (erreur RLS/400/500/token) → l'utilisateur voyait quand même « ✅ sauvegardé » alors que **rien n'était persisté** → donnée perdue au rechargement (contrairement aux commandes, protégées par la file d'attente). Correctif : helper **`sbSaveEchec(type,data,ctx,status,detail)`** (~ligne 2832) qui met en **file d'attente offline** (rejouée au retour réseau, `offlineQueueFlush` étendu aux types `produit/vehicule/entretien/livreur`) **+ toast d'alerte rouge** visible. Les 5 fonctions appellent désormais ce helper sur `!resp.ok` ET sur catch réseau.
- **FIX (v7.5.68) — suppression de produit non persistée + crash** : `supprimerProduit` (~ligne 11497) ne supprimait qu'en mémoire (produit **réapparaissait au rechargement**) et plantait sur `p.nom` si `p` introuvable. Correctif : garde `if(!p) return`, appel `sbDeleteProduit(id)`, MAJ du cache `mx_produits_custom`, `buildProduitOptions()`.
- **FIX (v7.5.68) — SMS veille >160 car = 2 crédits OVH** : le template `veille` de `send-cmd-sms` (~150 + lien confirmer.html ~86 car) partait en **2 segments = 2 crédits/SMS** (×2 sur le poste SMS le plus volumineux ; ~45 crédits restants → 2 tournées pouvaient vider le stock). Refonte du template (retrait du nom produit + phrase « appel 1h avant » + nom Maxiconfort déjà porté par le sender) + `dateFr` en **JJ/MM** : « Bonjour {prénom}, livraison prevue demain {JJ/MM}. Serez-vous la ? {lien} ». Mesuré ≤156 car même pour « Jean-Baptiste » → **1 SMS = 1 crédit**. Edge Function `send-cmd-sms` redéployée.
- **⚠️ Backlog issu de l'audit (non corrigé, à arbitrer) :**
  - 🔴 **Sécurité structurelle** : tout le RLS repose sur `x-app-secret` en clair dans le HTML public (`~ligne 2756`) — prouvé exploitable (lecture des 254 commandes : nom/tél/adresse/prix, + écriture/suppression). Inhérent à une app 100 % front. Vraie solution = **Supabase Auth (1 compte/personne) + RLS par `auth.uid()`**. Chantier de fond à planifier (RGPD).
  - ✅ **Indicateur « Encaissé » — DÉCISION PRISE (Borhen, 18/06) = option B : « total des ventes réglées, prépayé inclus ».** Donc le prépayé LeBonCoin reste compté (comportement déjà en place, rien à changer). **Fix v7.5.69** : `especesRemises` (~10002) utilise désormais `getMontantEspecesCmd` (inclut la part espèces d'un Mixte) pour être cohérent avec `especesCollectees` (~9987) — avant, la part espèces des Mixte gonflait « espèces en attente ». **Vérifié en base le 18/06 : 0 commande `stpaie='Partiel'`, 2 Mixte (toutes 'Payé' plein).** Donc le bug « Mixte partiel exclu des totaux » (filtres `stpaie==='Payé'` stricts dans ~12 calculs : 4989/5038/5039/5159/9726/9747/9860/9911/10285/10431/13751/14062) est **purement théorique aujourd'hui** — NON corrigé volontairement (réécrire 12 calculs financiers pour un cas à 0 occurrence = risque > bénéfice). ⚠️ **Si Borhen commence un jour à encaisser des paiements partiels**, ces 12 calculs (qui font `montantEnc||prix` avec repli prix) sous/sur-compteront → à traiter à ce moment-là avec un helper `montantRegle` (Payé→montantEnc||prix ; Partiel→montantEnc sans repli).
  - 🟠 **Sync stop par nom de client homonyme** (~3789) : deux clients de même nom → mauvais prix/statut propagé sur le stop (caisse faussée en silence). Reco : match strict sur `cmdId`.
  - 🟡 Décalage fuseau Paris/UTC sur les compteurs « Aujourd'hui » entre 22h-minuit (`toISOString().split('T')[0]`, ~4937 et autres) ; CA `NaN` si `prix===undefined` ; `montant_enc:||0` fragile si objet partiel passé à `sbSaveCommande` ; split de tournée qui duplique la logique d'ID au lieu d'appeler `nextTourneeId()` ; colonne fantôme `mode_paie` ; vocabulaire « Brevo » résiduel (noms de fonctions, message test) sans impact technique.

## État au 17/06/2026 (version v7.5.66)
- **FIX CRITIQUE — tournées qui "disparaissaient" (v7.5.66)** : l'ID d'une nouvelle tournée était `'T-' + (tournees.length + 1)`. Avec 21 tournées mais un trou de numérotation (max = T-22), `length+1` = **22** → la nouvelle tournée prenait l'id **T-22 existant** et l'**ÉCRASAIT** en base (UPSERT `merge-duplicates`). Donc chaque nouvelle tournée écrasait T-22 : les tours des 14/15/16/06 ont été écrasés un par un, ne laissant que le dernier (T-22 re-daté 17/06, created_at incohérent 22/05). Correctif : helper **`nextTourneeId()`** = `max(numéros existants) + 1` (logique déjà éprouvée ligne ~17099 pour le split de tournée), utilisé dans `creerTournee` (~8073) ET `suiviValiderAjoutTournee` (~6111). **Aucune donnée perdue côté ventes/CA** : les commandes (statut livré, date_livraison, montantEnc) sont intactes ; seuls les *regroupements* de tournée des jours écrasés sont perdus (reconstructibles via commandes). La page **Encaissements** (`finRender`/`finFiltrer`) lit les **commandes par date_livraison**, pas les tournées → non impactée. **Bonus** : tri de la liste des tournées passé en **date décroissante** (avant : par statut, les terminées récentes étaient reléguées en bas et semblaient disparues). ⚠️ Préview instable pendant le dev (timeouts) — fix validé par revue de code (mirroir ligne 17099) + JS qui parse (nextTourneeId callable).
## État au 17/06/2026 (version v7.5.67)
- **Signature : 3 choix de paiement, ne plus rien présumer (v7.5.67)** — corrige une fausse hypothèse de v7.5.56. **Règle réelle confirmée par Borhen** : une commande **marketplace n'est PAS toujours prépayée**. Seules les **expéditions nationales hors Île-de-France** (GLS) sont **obligatoirement payées d'avance** ; **certaines** LeBonCoin IDF sont payées via LeBonCoin ; **le reste des LeBonCoin IDF** (livrées par RANOU) est **payé à la livraison** (espèces/CB/mixte). v7.5.56 pré-cochait « Déjà payé » dès que l'origine était une marketplace → fausse la caisse (l'app croyait « rien à encaisser » alors que le livreur devait encaisser). Refonte : à la signature on ne **présume rien**, le livreur choisit parmi **3 boutons** — **✅ Encaissé** (puis 💵 Espèces / 💳 CB / 🔀 Mixte), **📦 Déjà payé** (prépayé en ligne, rien à encaisser ; `paie`=plateforme si marketplace sinon « Déjà payé », `stpaie`=Payé, **pas** compté en espèces), **❌ Non payé** (livré mais non réglé = créance, `stpaie`=Non payé, `montantEnc`=0). Pour une commande marketplace : simple **rappel** jaune (« si déjà payée en ligne, choisissez Déjà payé »), **aucune** pré-sélection ; défaut = Encaissé + Espèces pour toutes les commandes. `validerSig` a désormais 3 branches explicites (prépayé / encaissé / non payé) côté stop ET commande ; reset complet à la fermeture (`sigPrepaidMode`, `sigCmdMarketplace`, surlignage des 3 boutons). Testé navigateur (écritures bloquées) : transitions d'état OK (Encaissé→prepaid=null ; Déjà payé marketplace→plateforme ; Déjà payé non-mkt→« Déjà payé » ; Non payé→tout à zéro) + surlignage UI (1 seul bouton actif, modes Espèces/CB/Mixte visibles **seulement** pour Encaissé). Voir mémoire `paiement-leboncoin-regle.md`. ⚠️ v7.5.56 (ci-dessous) décrit l'ancien comportement erroné — conservé pour historique mais **remplacé** par v7.5.67.

## État au 16/06/2026 (version v7.5.65)
- **SMS approche enfin opérationnel (v7.5.65)** : le SMS « proche » (approche ~30 min) n'était JAMAIS parti malgré GPS actif + tournée du jour + livreur à côté du client. Cause racine : **mismatch de source du nom livreur**. Le GPS envoie ses positions via `livNom` (`gpsEnvoyerPosition`), mais `verifierProximiteClients` identifie le livreur via `geofenceGetLivreurActuel()` qui lisait `livreurActuel`/localStorage (`livreur_connecte`/`current_user`) — **jamais `livNom`**. Sur le téléphone du livreur, `livNom`='RANOU' (GPS OK) mais `geofenceGetLivreurActuel()` renvoyait `null` → `verifierProximiteClients` sortait immédiatement → aucun SMS. Correctif : `geofenceGetLivreurActuel()` lit **`livNom` en premier**. Testé bout-en-bout (livreur à Rosny, position GPS réelle 48.88,2.47) : stops géocodés en mémoire, SMS proche déclenché pour #1282 (Ali Rosny) + #1280 (Mr Alloux Fontenay), les 2 clients à ≤30 min. ⚠️ Le livreur doit avoir la version ≥ v7.5.65 sur son tél (recharger 2×) pour que ça marche en tournée. Brevo réactivé, donc l'envoi part.
## État au 16/06/2026 (version v7.5.64)
- **Garde-fou date livraison < commande (v7.5.64)** : récurrent — une commande saisie avec une **mauvaise année de livraison** (ex #1290 TikTok Shop : livraison `2024-06-18` au lieu de `2026`, date_commande `2026-06-16`) tombait sous le filtre anti-rétroactif (`isVraieCmd` : `date_livraison >= date_commande`) et était **exclue du CA en silence** → « Aujourd'hui : 0 cmd » alors qu'une vente venait d'être ajoutée. Correctif `saveCmd` : si `date_livraison < date_commande` ET la case « saisie rétroactive » (`f-cmd-retro`) n'est PAS cochée → `confirm()` d'alerte (« Annuler = corriger / OK = rétroactif volontaire »). #1290 corrigé manuellement (livraison 2026-06-18) → CA jour repassé à 1 cmd/173€. Rappel : le filtre anti-rétroactif reste **voulu** par Borhen (ne pas le retirer) ; le garde-fou attrape juste les fautes de frappe à la source. Les commandes TikTok Shop sont saisies **manuellement** (pas d'import), donc le garde-fou les couvre.
## État au 16/06/2026 (version v7.5.63)
- **GLS multi-colis : fix troncature du tracking (v7.5.63)** : à la création d'étiquette via le bouton app (`panelCreerEtiquetteGLS`), le front-end ne gardait que `data.trackId` (le **1er** colis) dans `c.trackingTransporteur`, alors que l'Edge Function enregistre bien la **liste complète** (`00L0OU3W,00L0OU3X`) en base. Du coup la **prochaine `sbSaveCommande`** (clic « 💾 SMS », modif, etc.) réécrasait les 2 colis par 1 seul → le 2ᵉ tracking perdu. Cas réel #1290 (Ensemble 90×190 = 2 colis) : passé de `00L0OU3W,00L0OU3X` à `00L0OU3W`. Correctif : `panelCreerEtiquetteGLS` construit `trackingComplet = data.parcels.map(p=>p.trackId).join(',')`. #1290 restauré manuellement. **Affichage (v7.5.63)** : le panneau commande montre désormais « 📦 N colis enregistrés chez GLS » + tous les n° de suivi (le champ input étroit n'affichait que le 1er). Suspects historiques (#1165/#1147/#1118/#1018 : 1 track pour un ensemble) tous **livrés** → pas d'action (le 2ᵉ n° perdu est irrécupérable, mais colis déjà livrés).
## État au 15/06/2026 (version v7.5.62)
- **Reports re-planifiables dans le Suivi (v7.5.62)** : sur le Suivi logistique, les commandes **non livrées restées dans la tournée du jour/passée** (reports) avaient leur case à cocher **désactivée** (`cmdsDansTournee` bloquait dès qu'une commande était dans *n'importe quelle* tournée) → impossible de les ajouter à la tournée du lendemain. Cas réel : T-22 (15/06, « préparée », 5 livrés/12) contenait #1282, #1280, #1279, #1277, #1274, #1272, #1260 non livrés, date déjà passée au 16/06 mais bloqués. Correctif `suiviAppliquerFiltres` : `cmdsDansTournee` ne bloque QUE les tournées **futures** (`t.date > todayStr`) ; une commande dans une tournée du jour/passée reste cochable. **Optimisation `suiviValiderAjoutTournee`** : à l'ajout, `c.date = date` (alignée sur la tournée cible) ET la commande est **retirée des autres tournées** où elle traînait non livrée (`stops.filter` qui préserve les stops `livré` = historique intact), tournées modifiées re-sauvées. Vérifié : #1282 devient cochable ; retrait de T-22 (12→11) en préservant les 5 livrés.
## État au 15/06/2026 (version v7.5.61)
- **Bouton envoi manuel SMS confirmation (v7.5.61)** : Borhen organise souvent sa tournée **après 19h** (le cron veille `sms-auto` à 19h Paris rate alors les stops ajoutés ensuite). Nouveau bouton vert **« 📲 Envoyer les SMS de confirmation (demain) »** en haut de l'onglet **Notifications → SMS Veille** : `envoyerVeilleConfirmation()` appelle `sms-auto` (`phase:veille`) côté serveur = même action que le cron (message « le livreur appelle 1h avant » + lien `confirmer.html` + **dedupe `sms_envoyes`** donc pas de doublon). Toast récap (envoyés/ignorés/échecs). ⚠️ L'ancien bouton « 📱 Envoyer SMS veille » (`smsEnvoiVeille`/`brevoEnvoiVeille`, front-end `tpl-veille`) envoie encore l'**ancien message SANS lien** — le bouton vert serveur est le moyen recommandé (banderole l'indique). Message veille mis à jour (15/06) : plus de faux créneau « 08:00-17:00 », remplacé par « Le livreur vous appellera environ 1h avant son passage ».
## État au 15/06/2026 (version v7.5.60)
- **Confirmation de présence par lien (v7.5.60)** : pour réduire les livraisons ratées (client absent), le **SMS veille** contient désormais un lien `confirmer.html?t=<tracking_token>`. Le client ouvre → page mobile (2 boutons **✅ Je serai présent** / **📅 Je ne serai pas là**) → l'Edge Function `confirmer-presence` écrit `commandes.confirmation_presence` (`confirme`/`reporter`) + `confirmation_at`, et **alerte Borhen par SMS Brevo si « reporter »** (annulation actionnable). Côté admin : badge `confirmBadge(c)` (🟢 Présence OK / 🔴 Absent — reporter) dans le **Suivi logistique** (sous le statut) et le **panneau commande** (panel-sub). Borhen sécurise sa tournée la veille en voyant qui a confirmé avant le départ du livreur. **Archi** (aucune nouvelle table, calqué sur track.html/facture.html) : 2 colonnes `confirmation_presence`/`confirmation_at` sur `commandes` (+ `CMD_SELECT_COLS` + 4 mappings + champ `confirmationPresence`) ; page publique lit par `tracking_token` (RLS publique) ; écriture via Edge Function `confirmer-presence` (service role, sécurisée par token). Lien injecté dans `send-cmd-sms` template `veille` (`lienConfirm`). `confirmer.html` hors cache SW. MVP : pas de retrait auto de tournée (Borhen garde la main), pas de relance auto matin — extensions possibles (créneau matin/aprem, choix d'un autre jour, filtre « non confirmés »). Testé bout-en-bout : page rend, clic écrit en base, badge s'affiche.
## État au 15/06/2026 (version v7.5.58)
- **GLS colis = toutes les lignes (gls-create-shipment v5.11, 15/06)** : une commande site Maxiconfort « matelas + sommier » (2 `lignes`) ne générait l'étiquette GLS que pour le matelas (1 colis) — le sommier était absent. Cause : `buildShipmentPayload` calculait les colis sur le **résumé `cmd.produit`** (qui n'affiche que le 1er article + « (+N autres) ») au lieu de `cmd.lignes`. Les `lignes` étaient pourtant complètes (vérifié : `shopify-sync` mappe bien tous les `line_items`). Correctif : `buildColis(cmd)` parcourt **chaque ligne × quantité**, applique `detectMultiColis` au nom de CHAQUE ligne, cumule ; Note1 = produit réel du colis ; repli sur `cmd.produit` si pas de lignes. Testé dryRun. **Règle colis sommier confirmée par Borhen (v5.12, 15/06)** : sommier **BOIS À LATTES = 1 colis** (transport à plat) ; sommier **TAPISSIER = 2 demi-colis** pour 120/190, 140/190, 140/200, 160/200, 180/200 (autres tailles ex 90 = 1 colis). ⚠️ Piège : matcher `lattes` et NON `bois` dans `MULTICOLIS_RULES` (les tapissiers ont « Pieds Bois » dans le nom). Vérifié : #1027 matelas+sommier tapissier 140×190 → 3 ; #1024 (2 mat + 2 sommiers bois à lattes) → 4 ; #1025 (5 sommiers tapissier 160×200) → 10 ; #1028 matelas seul → 1.
- **SMS approche = ~30 min de route réelle (v7.5.58)** : le SMS « proche » se déclenchait à **< 1 km à vol d'oiseau** (~5 min) — bien trop tard pour un message « arrive dans 30 min ». Borhen : 1 km trop près. Refonte `verifierProximiteClients` → `smsProcheVerifierEta(s, latLiv, lngLiv, latC, lngC, distBrut)` : déclenchement basé sur le **TEMPS de trajet**, pas la distance. Estimation locale instantanée (`distBrut/0.75/SMS_PROCHE_VITESSE_KMH*60`, vitesse 35 km/h IDF → ~30 min ≈ 13 km à vol d'oiseau) **toujours fiable sans réseau**, affinée par **OSRM** (`/route`, timeout 5 s) si dispo. Déclenche si ETA ≤ `SMS_PROCHE_ETA_MIN` (=30, réglable). Pré-filtre vol d'oiseau > 30 km = skip (pas d'OSRM inutile) ; throttle OSRM 1/90 s par client (`s._etaCheckMs`) ; dedupe 1 h (`_gfProximityCache`) + serveur (`sms_envoyes`). ⚠️ OSRM public `/route` souvent très lent (>30 s observé) → l'estimation vitesse est la voie principale en pratique. Testé : Villiers 3 km + Créteil 12 km → SMS ; Versailles 33 km + Mantes 65 km → pas de SMS. Réglages en haut de fichier : `SMS_PROCHE_ETA_MIN` (minutes) et `SMS_PROCHE_VITESSE_KMH`.
- **SMS approche 30 min réparé (v7.5.57)** : le SMS « proche » (type `proche`, déclenché auto par proximité GPS via `verifierProximiteClients`, ~ligne 15360) **n'était JAMAIS parti** (0 en historique). Cause : la fonction lit `s._lat || s.lat` sur le stop de tournée pour calculer la distance livreur↔client, mais **les stops de tournée n'ont aucune coordonnée** (champs lat/lng absents — le géocodage ne se faisait que dans `optimStops` de la page Optimiser, jamais sauvé dans les stops). Sans coords → `if (!latC) return` → SMS sauté à chaque fois. Correctif : si coords manquantes, géocoder l'adresse du stop en arrière-plan (1 stop par passage GPS pour respecter la limite Nominatim, drapeau `_gfGeocodeLanceCeTour`), résultat caché dans `s._lat/s._lng` (`geocoderAdresse` a un repli code postal). Testé : 1er passage géocode Draveil (48.683,2.389), 2e passage à proximité → `sendCmdSmsAsync('proche')` déclenché. ⚠️ Nécessite GPS livreur actif (confirmé : `gps_positions` RANOU à jour) + passage à <1km du client. **État SMS au 15/06 : Brevo RÉACTIVÉ** (départ 15/06 05:00 et veille 14/06 partis « envoyé »). Types `depart`/`veille` OK ; `proche` désormais possible ; `livre` (confirmation) envoyé via `brevoSendSMS` dans `validerSig` (pas loggé en historique avec le type) ; `expedition` GLS à re-tester (dernier échec 12/06 pendant le blocage Brevo).
- **⛔ REMPLACÉ par v7.5.67 — Signature : marketplace = déjà payé par défaut (v7.5.56)** : *prémisse FAUSSE (« marketplace toujours payée en ligne ») — voir v7.5.67. Conservé pour historique.* À la signature d'une livraison, le mode était pré-coché « ✅ Oui encaissé + Espèces ». Pour une commande **marketplace** (toujours payée en ligne avant le départ du dépôt, confirmé Borhen), si le livreur signait sans changer → « Espèces encaissé » enregistré à tort (fausse la caisse : l'app croit que le livreur a le cash). Correctif : `cmdOrigineMarketplace(cmd)` détecte l'origine marketplace (`MARKETPLACES_ORIGINES` : LeBonCoin, Amazon, Cdiscount, Fnac Darty, Rakuten, Rue du Commerce, Ubaldi, ManoMano, Conforama, But, La Redoute, Maison du Monde, Boulanger, Leroy Merlin, eBay, Wish, AliExpress, Vinted, Facebook Marketplace, TikTok Shop, Instagram Shop ; repli sur `paie` si déjà une plateforme). À l'ouverture de la signature (`setLvSt` branche `livré`), si marketplace : `sigPrepaidMode = origine`, bandeau vert « ✅ Déjà payé en ligne via X — rien à encaisser », la question « Paiement reçu ? » est masquée. `validerSig` enregistre alors `paie = plateforme`, `stpaie = Payé`, `montantEnc = prix` (compté en prépayé, jamais en espèces/CB). Les commandes **encaissées sur place** (Site/SAV/appel direct…) gardent le défaut Espèces inchangé. `sigPrepaidMode` remis à null + UI restaurée à chaque validation/ouverture. Testé navigateur (écritures DB bloquées) : TRC LeBonCoin → `LeBonCoin/Payé/129` ; SAV non-marketplace → flux Espèces/CB intact. Yannick #1218 corrigé manuellement en CB (commande + stop T-21) le 13/06.
- **Facture sans mode de règlement (v7.5.55)** : le badge de la facture affiche `FACTURE ACQUITTÉE` (si `stpaie=Payé`) ou `À RÉGLER`, **sans** le mode `paie` interne. Raison : `paie` est peu fiable sur une facture client (reste souvent sur « Espèces » par défaut pour les commandes marketplace prépayées où le livreur n'encaisse rien — ex. LeBonCoin/CB affiché « Espèces »), et le mode de paiement interne n'a pas à figurer sur une facture. Retiré dans `buildFactureHTML` (app) ET `facture.html`. Si un jour Borhen veut le vrai mode, il faudra d'abord fiabiliser le champ `paie` des commandes marketplace.
- **Facture client à la demande (v7.5.54)** : bouton **📄 Facture** dans le panneau de chaque commande (à côté de Bon/Reçu). Mentions légales = `LITIGE_BMS` (BMS, SAS, 60 rue François 1er 75008 Paris, RCS Paris 993 863 687, TVA `FR29993863687`, **TVA 20%** : HT/TVA/TTC calculés depuis `prix` TTC ; badge ACQUITTÉE si `stpaie=Payé`, sinon À RÉGLER). Le bouton ouvre le modal `m-facture` (4 actions) : 🖨️ Imprimer/PDF (`buildFactureHTML` → fenêtre + window.print), 📥 Télécharger (Blob `.html` `Facture_F<AAAA>-<id>.html`), 💬 WhatsApp (`wa.me/<tel intl>?text=...`), 📱 SMS (`sms:` + copie presse-papier). Les 2 envois transmettent un **lien** vers la page publique **`facture.html?t=<tracking_token>`** (nouveau fichier, modèle calqué sur `track.html` : lecture commande par `tracking_token` + header `x-tracking-token`, RLS publique, **aucun secret, aucune nouvelle table** — toutes les commandes ont déjà un token, 0 sans). N° facture = `F<année cmd>-<id sans #>`. La facture des litiges GLS (`litigeFactureVente`) reste séparée mais partage les mêmes mentions BMS. `facture.html` n'est pas dans le cache SW (page publique fraîche, comme track.html).
- **PIN admin = base source de vérité (v7.5.52)** : changer un PIN en base (table `parametres`, ex `pin_admin`) n'avait AUCUN effet sur le login. Cause : (1) l'écran de login lit `localStorage.getItem('pin_admin') || '1234'` au démarrage (clé jamais écrite → toujours `1234`) ; (2) `sbLoadParametres()` (qui lit le PIN en base) ne tournait que dans `buildUI()`, soit APRÈS le login ; (3) `savePin()` n'écrivait qu'en localStorage, jamais en base. Les deux systèmes (local vs base) étaient déconnectés. Correctifs : `sbLoadParametres` appelé au `DOMContentLoaded` (avant login) + écrit les clés `localStorage.pin_admin/collab/livreur` (instant au prochain démarrage) ; `savePin` fait aussi `sbSaveParametre('pin_'+r, v)` (propagation cross-device) ; déblocage d'urgence SAS (`sasDeblocageAdmin`) utilise `PINS.admin` au lieu de `1234` en dur. Testé : appareil neuf (localStorage vidé) → PINS.admin=0402 au démarrage, livreur reste 9012. **PIN admin actuel = `0402`** (collab 5678, livreur 9012).
- **Polling encaissement fiable (v7.5.51)** : le règlement validé par le livreur (CB/Espèces/Mixte/Déjà payé) ne remontait pas en direct côté admin (page Encaissements + tournée) pour les commandes **anciennes** livrées le jour même. Cause : le polling 10 s (`startPolling`, ~ligne 3654) ne récupérait que les **50 commandes les plus récemment CRÉÉES** (`order=created_at.desc&limit=50`) — une commande passée il y a 2-3 semaines puis livrée aujourd'hui était hors fenêtre (vérifié : `#1172` livrée CB hors des 50). Correctif : tri par **`updated_at.desc.nullslast&limit=80`** → toute commande que le livreur vient de modifier remonte en tête et est rafraîchie en 10 s. Le bloc `_syncNeeded` propage ensuite `paie/stpaie` commande→stop, donc la tournée se corrige aussi. Un seul changement règle les 3 symptômes (admin, encaissement, tournée). Le canal Realtime (autre voie) mappe déjà bien `paie/stpaie/montantEnc` mais le WebSocket est instable → le polling est le filet de sécurité. `sbSaveCommande` met toujours `updated_at` (0 commande NULL en base). Note : `finRender` utilise `montantEnc || prix` (un `montant_enc=0` sur une commande CB/Payé retombe sur le prix, pas un bug d'affichage).
- **✅ TOUS les SMS transactionnels migrés Brevo → OVH (16/06)** : Brevo bloqué depuis le 12/06, expéditeur OVH `MAXICONFORT` validé le 16/06 → bascule complète. Helper partagé **`supabase/functions/_shared/ovh-sms.ts`** (`envoyerSMSOVH(tel, message, {noStop, sender, forceNumeroCourt})` : signature API OVH, normalisation `+336/7`, `sansAccents`, service `sms-rw88345-1`, sender `MAXICONFORT`, **transactionnel = `noStopClause:true`** = pas de mention STOP). Importé par les 5 points d'envoi, tous redéployés : (1) **send-cmd-sms** (veille/depart/route/proche/expedition — couvre aussi `sms-auto` cron + `gls-create-shipment` qui délèguent à elle) ; (2) **send-sms** (RÉÉCRITE, code source perdu — point d'entrée du front `brevoSendSMS` : confirmation livraison à la signature, test, rappel entretien, envoi groupé, veille legacy ; contrat inchangé `{tel,message}→{success}`) ; (3) **sms-veille** v2 (legacy ; creds Supabase hardcodés retirés + faux créneau « 08:00-17:00 » supprimé) ; (4) **gls-sync** v13 (alerte colis bloqué) ; (5) **confirmer-presence** (alerte report présence). Plus AUCUN `api.brevo.com`/`BREVO_API_KEY` dans les Edge Functions (vérifié). **Front HTML NON modifié** (il appelle déjà `send-sms` ; `brevoSendSMS` gardée par compat → pas de bump cache). Secret `OVH_SMS_SERVICE=sms-rw88345-1` ajouté. Testé : `send-sms`→SMS reçu (success:true) ; `send-cmd-sms`/`confirmer-presence` démarrent OK. ⚠️ **Crédits OVH = 45** → recharger avant la prod (1 SMS transactionnel = 1 crédit). Campagnes marketing (`sms-ovh`) gardent la clause STOP.
- **SMS OVH opérationnel (12/06 soir)** : nouvelle Edge Function `sms-ovh` v1.1 (actions `status`/`test`/`campagne`/`tick`, signature API OVH, lots de 50, garde-fou `confirm:true` au-delà de 50 numéros, mention STOP + blacklist gérées par OVH via `noStopClause:false`, log dans `sms_historique` type `campagne`). Service OVH `sms-rw88345-1`, secrets `OVH_APP_KEY/OVH_APP_SECRET/OVH_CONSUMER_KEY/OVH_SMS_SENDER` posés. ~99 crédits restants (1 crédit = 1 SMS FR ≤160 car. ; la clause STOP ajoute ~11 car. → message ≤ ~149 car.). SMS test reçu OK. Sert aussi de plan B à Brevo (bloqué) et de base à la migration SMS.
- **Campagne auto en attente (12/06, MAJ 15/06)** : table `campagnes_sms` (RLS sans policy = service role only) + cron `sms-ovh-tick-30min` (`*/30 7-18 * * 1-6` UTC). Le tick envoie la campagne `attente_expediteur` la plus ancienne dès que : expéditeur `MAXICONFORT` validé par OVH (`enable`) + fenêtre légale 10h-20h Paris lun-sam. Anti double-envoi via UPDATE conditionnel `statut='en_cours'`. SMS récap/alerte à Borhen toujours en **numéro court** (`forceNumeroCourt`, sms-ovh v1.2) pour partir même si l'alpha est bloqué. Campagne `camp_test50_relance` (50 « à relancer », message -10% validé, fixe `09 72 14 98 05`).
- **✅ Expéditeur MAXICONFORT VALIDÉ + 1re campagne envoyée (16/06, 14h30)** : après 2 refus OVH (1. capture IONOS jugée insuffisante ; 2. demande explicite d'un **Kbis BMS < 3 mois + pièce d'identité d'un dirigeant**), Borhen a déposé via `/sms/sms-rw88345-1/senders/add` le **Kbis BMS + titre de séjour** → sender passé `enable`. Le cron `sms-ovh-tick-30min` a alors envoyé seul `camp_test50_relance` : **50 SMS, 0 blacklist/invalide**, expéditeur « MAXICONFORT » affiché, SMS récap reçu par Borhen. **Crédits OVH : 45 restants.** Leçons : (1) OVH exige un **Kbis société** (pas une preuve de domaine) pour un alpha de marque ; (2) le repli numéro court est inutilisable pour du marketing (OVH bloque `"cannot send contact information with shortcode"` dès qu'il y a site/tél) ; (3) le champ Description du formulaire OVH refuse `-` et `.`. Registrar domaine = IONOS SE. **Prochaine étape** : mesurer les conversions du test (commandes site/tél venant des 50), puis si OK → recharger crédits OVH (~36 €/500) + campagne sur les **268 « à relancer » restants + 67 fidèles**, puis migrer les SMS transactionnels Brevo→OVH (Brevo toujours bloqué).
- **Base clients « les autres » analysée (12/06)** : Google Sheets « Création Tableau Commandes Paysage » (commandes terrain 10/02→06/05/2026) → 576 commandes, **492 mobiles uniques** segmentés dans `contacts_sms_base_autres.csv` (local, dans `.gitignore` avec `base_clients.xlsx` — données personnelles, JAMAIS commit) : 67 fidèles (2+ cmd), 106 récents (<30 j), 318 à relancer (30-90 j). Campagne SMS prévue via `sms-ovh` (test 50 d'abord, attendre validation expéditeur, envoi 10h-20h hors dimanche/férié).
- **gls-create-shipment v5.10 (12/06)** : refonte fiabilité après panne « CORS blocked » sur création d'étiquette. Causes réelles découvertes : (1) **Note1 max 50 caractères** chez GLS (le code tronquait à 60) → rejet des produits au nom long (ex #1227 lit superposé) ; (2) GLS renvoie ses erreurs de validation dans des **en-têtes HTTP malformés** que le fetch Deno refuse de parser (h2 « unspecific protocol error », h1 « invalid HTTP header parsed ») → crash 500 sans CORS. Correctifs : appel GLS en **HTTP/1.0 brut** via `Deno.connectTls` (parsing tolérant, message d'erreur extrait des en-têtes `message:`/`error:`), try/catch global (toute erreur → JSON+CORS lisible dans l'app), `glsSafe()` translittère tous les champs texte en ASCII (accents/×/— → e/x/-) avec limites sûres (Note1 50, Name1/City/Street 40). ⚠️ AUCUN retry après envoi (risque doublon facturable). Debug : les logs se lisent via `GET api.supabase.com/v1/projects/<ref>/analytics/endpoints/logs.all?sql=...&iso_timestamp_start=...` avec le PAT (`SUPABASE_ACCESS_TOKEN` désormais dans `.env`).
- **⚠️ SMS Brevo BLOQUÉS depuis le 12/06 ~16h** : Brevo répond `Your SMS sending status is not yet activated. Please contact us at contact@sendinblue.com` (HTTP 400) — le compte a été désactivé pour l'envoi SMS entre 10h44 (dernier SMS parti) et 16h20. TOUS les SMS sont concernés (expédition, veille 19h, départ 7h). Action Borhen : vérifier le compte Brevo / les contacter. Le SMS expédition de #1227 n'est pas parti (loggé « échec » dans `sms_historique`) ; le renvoyer après réactivation via l'app (la dedupe ne bloque pas, seuls les succès sont enregistrés dans `commandes.sms_envoyes`). Renforce le backlog « Migration SMS Allmysms/OVH ».
- **Optimisation tournée fiable (v7.5.50)** : 4 correctifs. (1) `geocoderAdresse` a un repli **code postal seul** si l'adresse complète est introuvable sur Nominatim (ex : faute de frappe « cheuvreuse » → CP 78460 = Chevreuse) — avant, le stop atterrissait sur des coordonnées par défaut (Paris) et l'ordre devenait absurde. (2) Badge orange « 📍 Adresse imprécise » sur les stops localisés au centre de la commune (flag `geocodeApprox`) pour inciter à corriger l'adresse. (3) Géocodage **séquentiel** (1,05 s entre requêtes non cachées — Nominatim limite à 1 req/s, le parallèle échouait en silence). (4) Les ETA utilisent les durées **OSRM** via `optimDerniereMatrice` + tag `_mIdx` par stop (avant : recalcul vol d'oiseau qui écrasait les vraies durées) + timeout 20 s sur l'appel OSRM (sinon blocage infini si le serveur public ne répond pas). Fin de tournée : le 2-opt inclut le trajet vers le domicile du livreur — `livreur_RANOU_domicile` = Conflans-Sainte-Honorine (déjà en base, vérifié). Testé sur T-20 : Chevreuse regroupée avec Guyancourt, boucle est→sud→ouest→nord→Conflans.
- **Édition commande SAV (v7.5.47)** : les SAV Reprise/Remboursement (créés sans ligne produit) sont enfin modifiables : le bloc « Produits commandés » est masqué à l'édition d'un SAV (note explicative à la place), la validation « Ajoutez au moins un produit » est sautée, et `saveCmd` préserve produit/lignes/prix/remises/`origine` du SAV (le select Origine n'a pas d'option `SAV - ...` → il aurait effacé le marqueur et réintégré le SAV dans le CA). Bonus : `montantEnc` est désormais préservé sur TOUTE édition (avant, l'UPSERT le remettait à 0 → remboursements SAV et encaissements livreur perdus en base).
- **Alerte colis GLS bloqué (v7.5.49 + gls-sync v12)** : gls-sync (cron 2h) extrait la date du dernier scan de chaque colis (`UnitDetail.History[].Date`). Si un colis non livré n'a aucun scan depuis 4 jours (`stuckDays` paramétrable via body) → `commandes.gls_bloque=true` + `gls_dernier_scan` + **SMS Brevo à Borhen** (+33744289321, une seule fois par épisode — dedupe via l'ancien gls_bloque ; flag retombe si le colis bouge ou est livré). Front : bannière rouge dans le panneau commande + 🚨 colonne Tracking du Suivi logistique.
- **Litige GLS (v7.5.48)** : bouton ⚠️ dans le panneau d'une commande GLS avec tracking → modal dossier litige (type perte/avarie/retour, n° réclamation 13 chiffres, statut, description) + 3 documents en 1 clic : email d'ouverture (copié + mailto litiges@gls-france.com), lettre de réserves à faire signer au client (72h !), facture de vente **BMS TVA 20%** (SAS BMS, RCS Paris 993 863 687, 60 rue François 1er 75008 Paris — le compte GLS est au nom de BMS, confirmé par Borhen). 5 colonnes `litige_*` ajoutées à `commandes` + à `CMD_SELECT_COLS` + à `sbSaveCommande`. Date limite dépôt = ouverture + 40 jours ouvrés (calcul auto). ⚠️ N° TVA BMS `FR29993863687` calculé depuis le SIREN — à vérifier sur un document officiel.
- **Perf chargement commandes (v7.5.46)** : les 3 requêtes de chargement des commandes (initial, pagination, polling 30s) utilisent une liste explicite de colonnes (`CMD_SELECT_COLS`, 33 colonnes) au lieu de `select=*`, pour exclure `gls_pdf_base64` (PDF étiquette 50-400 Ko/cmd). Gain mesuré : 0,9 Mo → 216 Ko. Les PDF restent récupérés à la demande via l'action `getLabel` de `gls-create-shipment`. ⚠️ Toute nouvelle colonne de `commandes` utilisée par le mapping doit être ajoutée à `CMD_SELECT_COLS` (sinon HTTP 400 si nom faux, ou champ vide si oubli).
- **Rapport GLS du jour (v7.5.45)** : bouton 🖨️ page Suivi logistique → liste imprimable des étiquettes créées à une date (signatures chauffeur/expéditeur). Nouvelle colonne `commandes.gls_date_etiquette`, remplie par `gls-create-shipment` v5.9 (heure Paris). Les étiquettes d'avant le 11/06/2026 n'ont pas de date → absentes du rapport.
- **Stats Commandes (v7.5.44)** : les SAV (origine `SAV - ...`) sont exclus des badges Aujourd'hui/Semaine/Mois (pas des ventes). Données : 8 dates de livraison corrigées le 11/06 (année 2024/2025 saisie par erreur → exclues à tort du CA par le filtre anti-rétroactif).
- **Règle confirmée par Borhen (11/06/2026)** : les saisies rétroactives (date_livraison < date_commande) restent EXCLUES du CA des badges — ne pas « corriger » ce comportement.
- **Tournées** : refonte v7.5.41→43 (split 2 colonnes, smart actions, cards affichent l'ENCAISSÉ)
- **GLS** : `gls-create-shipment` v5.8 — multi-colis auto (120x190 ensemble = 3 colis [15,5,5], sommier seul = 2), PDF multi-pages, SMS expédition envoyé côté serveur. `gls-sync` v11 fonctionne (creds ShipIT-FARM) pour le suivi auto.
- **SMS automatisés** : Edge Function `sms-auto` + pg_cron — veille 19h Paris + départ 7h Paris (RANOU only, dedupe via `sms_envoyes`). SMS expédition GLS côté serveur. Plus de clients oubliés.
- **Autres Edge Functions** : send-cmd-sms (depart/route/expedition/proche/veille), shopify-sync (cron 10 min), whatsapp-receive (bot commandes IA Claude Haiku, multi-numéros), track-livreur (suivi temps réel client), webhook orders/create (note de préparation bundles)
- Modules en place : Santé société (trésorerie/encours marketplaces/multi-comptes bancaires), Suivi logistique, SAV 4 types (dont Remboursement à impact comptable négatif), optimisation tournée OSRM+2-opt, géofence entrepôt Noisy-le-Grand, app Android Capacitor (GPS background)

## Backlog connu
- Migration Supabase Publishable/Secret Keys 2026
- WhatsApp Business prod (sortir du Sandbox Twilio, rejoin `join join-around` tous les 72h)
- Migration SMS Allmysms/OVH (économie ~50%)
- Colonne `canal_saisie` sur commandes (mesurer l'apport du bot WhatsApp)
- Backup auto Supabase quotidien · UI templates SMS · refactor en modules

## Pièges connus
- PATCH Supabase via PowerShell : encoder UTF-8 manuellement (accents → HTTP 400) et URL-encoder `#` en `%23`
- Intercepteur fetch global ajoute `x-app-secret` aux URLs REST seulement (pas aux Edge Functions, sinon CORS bloque)
- Numéros Borhen : SMS = `+33 7 44 28 93 21` ; WhatsApp bot = `+33 7 51 56 31 13` (ligne SMS morte mais WA actif — ne pas toucher `ALLOWED_WHATSAPP_FROM`)

Périmètre : l'app de livraison + GLS intégré ici (étiquettes, suivi). Le site Shopify (`C:\Users\moind\maxiconfort`), l'outil LBC et TikTok ont leurs propres dossiers.
